# BDE Phase 1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Betriebsdatenerfassung Phase 1 — Terminal-UI (Scan-basiert), Leitstand-Cockpit (Polling), Mengenmeldung (Gut/Ausschuss), Buchungs-Event-Log mit State-Machine und Operator-Handover. Ohne Störungen und ohne SAGE-Rückmeldung (Phase 2).

**Architecture:** Fünf neue Entities (`BdeTerminal`, `BdeOperator`, `BdeActivity`, `BdeBooking`, `BdeBookingQuantity`). Event-Log-Modell mit `ParentBookingId`-Kette, DB-Level-Invarianten über gefilterte Unique-Indizes und Check-Constraints. Neue Rollen `bde_user`, `bde_shiftlead`, `bde_admin`. Eigener Service `BdeBookingService` kapselt die State-Machine. Cockpit-UI pollt `GET /api/bde/cockpit` alle 5 Sekunden.

**Tech Stack:** ASP.NET Core 10.0, EF Core 10.0, SQL Server, Bootstrap 5, jQuery/AJAX, xUnit + FluentAssertions + Moq + EF InMemory.

**Spec:** `docs/superpowers/specs/2026-04-14-bde-phase-1-design.md`

---

## File Structure

### Neue Dateien

| Datei | Verantwortung |
|-------|---------------|
| `IdealAkeWms/Models/BdeTerminal.cs` | Entity: Terminal-Konfiguration |
| `IdealAkeWms/Models/BdeOperator.cs` | Entity: Werker |
| `IdealAkeWms/Models/BdeActivity.cs` | Entity: ungeplante Tätigkeits-Kategorie |
| `IdealAkeWms/Models/BdeBooking.cs` | Entity: Event-Log-Buchung |
| `IdealAkeWms/Models/BdeBookingQuantity.cs` | Entity: Teil-/Abschluss-Mengen |
| `IdealAkeWms/Models/BdeBookingType.cs` | Enum: Setup/Production/Activity |
| `IdealAkeWms/Models/BdeBookingStatus.cs` | Enum: Running/Paused/Finished |
| `IdealAkeWms/Filters/RequireBdeUserAccessAttribute.cs` | Filter: `bde_user`+ |
| `IdealAkeWms/Filters/RequireBdeShiftleadAccessAttribute.cs` | Filter: `bde_shiftlead`+ |
| `IdealAkeWms/Filters/RequireBdeAdminAccessAttribute.cs` | Filter: `bde_admin`+ |
| `IdealAkeWms/Data/Repositories/IBdeOperatorRepository.cs` | Interface |
| `IdealAkeWms/Data/Repositories/BdeOperatorRepository.cs` | CRUD für Operatoren |
| `IdealAkeWms/Data/Repositories/IBdeActivityRepository.cs` | Interface |
| `IdealAkeWms/Data/Repositories/BdeActivityRepository.cs` | CRUD für Aktivitäten |
| `IdealAkeWms/Data/Repositories/IBdeTerminalRepository.cs` | Interface |
| `IdealAkeWms/Data/Repositories/BdeTerminalRepository.cs` | CRUD für Terminals |
| `IdealAkeWms/Data/Repositories/IBdeBookingRepository.cs` | Interface |
| `IdealAkeWms/Data/Repositories/BdeBookingRepository.cs` | Buchungs-Queries + atomare Mutationen |
| `IdealAkeWms/Services/IBdeBookingService.cs` | Interface für State-Machine |
| `IdealAkeWms/Services/BdeBookingService.cs` | State-Machine + Auto-End + Handover-Logik |
| `IdealAkeWms/Controllers/BdeTerminalController.cs` | Terminal-UI |
| `IdealAkeWms/Controllers/BdeCockpitController.cs` | Cockpit-UI |
| `IdealAkeWms/Controllers/BdeBookingsController.cs` | Buchungs-Liste + Korrektur |
| `IdealAkeWms/Controllers/BdeMasterDataController.cs` | Tabs: Operatoren, Aktivitäten, Terminals |
| `IdealAkeWms/Controllers/BdeApiController.cs` | JSON-Endpoints (Scan-Lookup, Cockpit-Poll) |
| `IdealAkeWms/Models/ViewModels/BdeTerminalViewModel.cs` | |
| `IdealAkeWms/Models/ViewModels/BdeOperatorEditViewModel.cs` | |
| `IdealAkeWms/Models/ViewModels/BdeActivityEditViewModel.cs` | |
| `IdealAkeWms/Models/ViewModels/BdeTerminalEditViewModel.cs` | |
| `IdealAkeWms/Models/ViewModels/BdeBookingListViewModel.cs` | |
| `IdealAkeWms/Models/ViewModels/BdeBookingEditViewModel.cs` | |
| `IdealAkeWms/Models/ViewModels/BdeQuantityDialogViewModel.cs` | |
| `IdealAkeWms/Models/ViewModels/BdeCockpitTileViewModel.cs` | |
| `IdealAkeWms/Views/BdeTerminal/Index.cshtml` | Haupt-Terminal-View |
| `IdealAkeWms/Views/BdeCockpit/Index.cshtml` | Kachel-Cockpit |
| `IdealAkeWms/Views/BdeBookings/Index.cshtml` | Buchungsliste |
| `IdealAkeWms/Views/BdeBookings/Edit.cshtml` | Korrektur-Formular |
| `IdealAkeWms/Views/BdeMasterData/Index.cshtml` | Tabs |
| `IdealAkeWms/Views/BdeMasterData/EditOperator.cshtml` | |
| `IdealAkeWms/Views/BdeMasterData/EditActivity.cshtml` | |
| `IdealAkeWms/Views/BdeMasterData/EditTerminal.cshtml` | |
| `IdealAkeWms/wwwroot/js/bde-terminal.js` | Terminal-Scan-Logik |
| `IdealAkeWms/wwwroot/js/bde-cockpit.js` | Polling-Render-Logik |
| `IdealAkeWms/wwwroot/css/bde.css` | Terminal- und Cockpit-Styles |
| `SQL/42_AddBde.sql` | DB-Migration (idempotent) |
| `IdealAkeWms.Tests/Models/BdeEntityTests.cs` | |
| `IdealAkeWms.Tests/Repositories/BdeBookingRepositoryTests.cs` | |
| `IdealAkeWms.Tests/Services/BdeBookingServiceTests.cs` | State-Machine-Tests |
| `IdealAkeWms.Tests/Controllers/BdeTerminalControllerTests.cs` | |
| `IdealAkeWms.Tests/Controllers/BdeApiControllerTests.cs` | Cockpit-JSON-Shape |

### Geänderte Dateien

| Datei | Änderung |
|-------|----------|
| `IdealAkeWms/Models/RoleKeys.cs` | +3 Konstanten |
| `IdealAkeWms/Services/ICurrentUserService.cs` | +3 Methoden |
| `IdealAkeWms/Services/CurrentUserService.cs` | +3 Implementierungen |
| `IdealAkeWms/Data/ApplicationDbContext.cs` | +5 `DbSet`, `OnModelCreating`-Config |
| `IdealAkeWms/Program.cs` | Rollen-Seeding +3, DI-Registrierungen |
| `IdealAkeWms/Views/Shared/_Layout.cshtml` | Navigation: BDE-Menü |
| `IdealAkeWms/Views/Help/Changelog.cshtml` | neue Version + BDE |
| `IdealAkeWms/Views/Help/Index.cshtml` | BDE-Abschnitt |
| `IdealAkeWms/AppVersion.cs` | Version hochzählen |
| `IdealAkeWms.Service/AppVersion.cs` | Version hochzählen |
| `SQL/00_FreshInstall.sql` | Konsolidierung BDE |
| `CLAUDE.md` | Rollen, Fallstricke |
| `PROJECT_STATUS.md` | BDE-Phase-1 Kapitel |
| `README.md` | Feature-Liste |

---

## Task 1: Enums + RoleKeys

**Files:**
- Create: `IdealAkeWms/Models/BdeBookingType.cs`
- Create: `IdealAkeWms/Models/BdeBookingStatus.cs`
- Modify: `IdealAkeWms/Models/RoleKeys.cs`

- [ ] **Step 1: Enum `BdeBookingType`**

Datei `IdealAkeWms/Models/BdeBookingType.cs`:

```csharp
namespace IdealAkeWms.Models;

public enum BdeBookingType : byte
{
    Setup = 1,
    Production = 2,
    Activity = 3
}
```

- [ ] **Step 2: Enum `BdeBookingStatus`**

Datei `IdealAkeWms/Models/BdeBookingStatus.cs`:

```csharp
namespace IdealAkeWms.Models;

public enum BdeBookingStatus : byte
{
    Running = 1,
    Paused = 2,
    Finished = 3
}
```

- [ ] **Step 3: `RoleKeys` erweitern**

Datei `IdealAkeWms/Models/RoleKeys.cs` ersetzen:

```csharp
namespace IdealAkeWms.Models;

public static class RoleKeys
{
    public const string Admin = "admin";
    public const string MasterData = "masterdata";
    public const string Picking = "picking";
    public const string Stock = "stock";
    public const string StockKeyUser = "stock_keyuser";
    public const string Tracking = "tracking";
    public const string Reporting = "reporting";
    public const string Leitstand = "leitstand";
    public const string BdeUser = "bde_user";
    public const string BdeShiftlead = "bde_shiftlead";
    public const string BdeAdmin = "bde_admin";
}
```

- [ ] **Step 4: Build**

```
dotnet build IdealAkeWms/IdealAkeWms.csproj
```

Expected: Build erfolgreich, 0 Warnings.

- [ ] **Step 5: Commit**

```bash
git add IdealAkeWms/Models/BdeBookingType.cs IdealAkeWms/Models/BdeBookingStatus.cs IdealAkeWms/Models/RoleKeys.cs
git commit -m "feat(bde): add enums and role keys for BDE phase 1"
```

---

## Task 2: Entities (Terminal, Operator, Activity)

**Files:**
- Create: `IdealAkeWms/Models/BdeTerminal.cs`
- Create: `IdealAkeWms/Models/BdeOperator.cs`
- Create: `IdealAkeWms/Models/BdeActivity.cs`

- [ ] **Step 1: `BdeTerminal`**

Datei `IdealAkeWms/Models/BdeTerminal.cs`:

```csharp
using System.ComponentModel.DataAnnotations;

namespace IdealAkeWms.Models;

public class BdeTerminal : AuditableEntity
{
    [Required]
    [Display(Name = "Terminal-Benutzer")]
    public int UserId { get; set; }

    public User User { get; set; } = null!;

    [Required]
    [Display(Name = "Default-Werkbank")]
    public int DefaultProductionWorkplaceId { get; set; }

    public ProductionWorkplace DefaultProductionWorkplace { get; set; } = null!;

    [StringLength(200)]
    [Display(Name = "Beschreibung")]
    public string? Description { get; set; }
}
```

- [ ] **Step 2: `BdeOperator`**

Datei `IdealAkeWms/Models/BdeOperator.cs`:

```csharp
using System.ComponentModel.DataAnnotations;

namespace IdealAkeWms.Models;

public class BdeOperator : AuditableEntity
{
    [Required(ErrorMessage = "Personalnummer ist erforderlich")]
    [StringLength(50)]
    [Display(Name = "Personalnummer")]
    public string PersonnelNumber { get; set; } = string.Empty;

    [Required(ErrorMessage = "Vorname ist erforderlich")]
    [StringLength(100)]
    [Display(Name = "Vorname")]
    public string FirstName { get; set; } = string.Empty;

    [Required(ErrorMessage = "Nachname ist erforderlich")]
    [StringLength(100)]
    [Display(Name = "Nachname")]
    public string LastName { get; set; } = string.Empty;

    [Display(Name = "Aktiv")]
    public bool IsActive { get; set; } = true;

    [Display(Name = "WMS-Benutzer")]
    public int? UserId { get; set; }

    public User? User { get; set; }

    public string DisplayName => $"{FirstName} {LastName}";
}
```

- [ ] **Step 3: `BdeActivity`**

Datei `IdealAkeWms/Models/BdeActivity.cs`:

```csharp
using System.ComponentModel.DataAnnotations;

namespace IdealAkeWms.Models;

public class BdeActivity : AuditableEntity
{
    [Required(ErrorMessage = "Code ist erforderlich")]
    [StringLength(20)]
    [Display(Name = "Code")]
    public string Code { get; set; } = string.Empty;

    [Required(ErrorMessage = "Bezeichnung ist erforderlich")]
    [StringLength(100)]
    [Display(Name = "Bezeichnung")]
    public string Name { get; set; } = string.Empty;

    [Display(Name = "Aktiv")]
    public bool IsActive { get; set; } = true;
}
```

- [ ] **Step 4: Build + Commit**

```bash
dotnet build IdealAkeWms/IdealAkeWms.csproj
git add IdealAkeWms/Models/BdeTerminal.cs IdealAkeWms/Models/BdeOperator.cs IdealAkeWms/Models/BdeActivity.cs
git commit -m "feat(bde): add Terminal, Operator, Activity entities"
```

---

## Task 3: Entities (Booking, BookingQuantity)

**Files:**
- Create: `IdealAkeWms/Models/BdeBooking.cs`
- Create: `IdealAkeWms/Models/BdeBookingQuantity.cs`

- [ ] **Step 1: `BdeBooking`**

Datei `IdealAkeWms/Models/BdeBooking.cs`:

```csharp
using System.ComponentModel.DataAnnotations;

namespace IdealAkeWms.Models;

public class BdeBooking : AuditableEntity
{
    [Required]
    public int BdeOperatorId { get; set; }
    public BdeOperator BdeOperator { get; set; } = null!;

    [Required]
    public int ProductionWorkplaceId { get; set; }
    public ProductionWorkplace ProductionWorkplace { get; set; } = null!;

    [Required]
    public int BdeTerminalId { get; set; }
    public BdeTerminal BdeTerminal { get; set; } = null!;

    public int? WorkOperationId { get; set; }
    public WorkOperation? WorkOperation { get; set; }

    public int? BdeActivityId { get; set; }
    public BdeActivity? BdeActivity { get; set; }

    [Required]
    public BdeBookingType BookingType { get; set; }

    [Required]
    public BdeBookingStatus Status { get; set; }

    [Required]
    public DateTime StartedAt { get; set; }

    public DateTime? EndedAt { get; set; }

    public bool IsCancelled { get; set; }

    [StringLength(500)]
    public string? CancellationReason { get; set; }

    public int? ParentBookingId { get; set; }
    public BdeBooking? ParentBooking { get; set; }

    public ICollection<BdeBookingQuantity> Quantities { get; set; } = new List<BdeBookingQuantity>();
}
```

- [ ] **Step 2: `BdeBookingQuantity`**

Datei `IdealAkeWms/Models/BdeBookingQuantity.cs`:

```csharp
using System.ComponentModel.DataAnnotations;

namespace IdealAkeWms.Models;

public class BdeBookingQuantity : AuditableEntity
{
    [Required]
    public int BdeBookingId { get; set; }
    public BdeBooking BdeBooking { get; set; } = null!;

    [Required]
    public int BdeOperatorId { get; set; }
    public BdeOperator BdeOperator { get; set; } = null!;

    [Display(Name = "Gutmenge")]
    public decimal GoodQuantity { get; set; }

    [Display(Name = "Ausschuss")]
    public decimal ScrapQuantity { get; set; }

    [Display(Name = "Abschluss-Meldung")]
    public bool IsFinal { get; set; }

    [Required]
    public DateTime ReportedAt { get; set; }
}
```

- [ ] **Step 3: Build + Commit**

```bash
dotnet build IdealAkeWms/IdealAkeWms.csproj
git add IdealAkeWms/Models/BdeBooking.cs IdealAkeWms/Models/BdeBookingQuantity.cs
git commit -m "feat(bde): add Booking and BookingQuantity entities"
```

---

## Task 4: DbContext + OnModelCreating

**Files:**
- Modify: `IdealAkeWms/Data/ApplicationDbContext.cs`

- [ ] **Step 1: DbSet-Properties ergänzen**

In `ApplicationDbContext` folgende `DbSet`-Properties hinzufügen (neben den bestehenden, alphabetisch einsortieren):

```csharp
public DbSet<BdeActivity> BdeActivities => Set<BdeActivity>();
public DbSet<BdeBooking> BdeBookings => Set<BdeBooking>();
public DbSet<BdeBookingQuantity> BdeBookingQuantities => Set<BdeBookingQuantity>();
public DbSet<BdeOperator> BdeOperators => Set<BdeOperator>();
public DbSet<BdeTerminal> BdeTerminals => Set<BdeTerminal>();
```

- [ ] **Step 2: `OnModelCreating`-Konfiguration ergänzen**

In `OnModelCreating` nach den bestehenden Konfigurationen folgenden Block einfügen:

```csharp
// BDE: Terminals
modelBuilder.Entity<BdeTerminal>(entity =>
{
    entity.HasOne(e => e.User).WithMany().HasForeignKey(e => e.UserId).OnDelete(DeleteBehavior.Restrict);
    entity.HasOne(e => e.DefaultProductionWorkplace).WithMany().HasForeignKey(e => e.DefaultProductionWorkplaceId).OnDelete(DeleteBehavior.Restrict);
    entity.HasIndex(e => e.UserId).IsUnique();
});

// BDE: Operators
modelBuilder.Entity<BdeOperator>(entity =>
{
    entity.HasOne(e => e.User).WithMany().HasForeignKey(e => e.UserId).OnDelete(DeleteBehavior.SetNull);
    entity.HasIndex(e => e.PersonnelNumber).IsUnique();
    entity.HasIndex(e => e.UserId).IsUnique().HasFilter("[UserId] IS NOT NULL");
});

// BDE: Activities
modelBuilder.Entity<BdeActivity>(entity =>
{
    entity.HasIndex(e => e.Code).IsUnique();
});

// BDE: Bookings
modelBuilder.Entity<BdeBooking>(entity =>
{
    entity.HasOne(e => e.BdeOperator).WithMany().HasForeignKey(e => e.BdeOperatorId).OnDelete(DeleteBehavior.Restrict);
    entity.HasOne(e => e.ProductionWorkplace).WithMany().HasForeignKey(e => e.ProductionWorkplaceId).OnDelete(DeleteBehavior.Restrict);
    entity.HasOne(e => e.BdeTerminal).WithMany().HasForeignKey(e => e.BdeTerminalId).OnDelete(DeleteBehavior.Restrict);
    entity.HasOne(e => e.WorkOperation).WithMany().HasForeignKey(e => e.WorkOperationId).OnDelete(DeleteBehavior.Restrict);
    entity.HasOne(e => e.BdeActivity).WithMany().HasForeignKey(e => e.BdeActivityId).OnDelete(DeleteBehavior.Restrict);
    entity.HasOne(e => e.ParentBooking).WithMany().HasForeignKey(e => e.ParentBookingId).OnDelete(DeleteBehavior.Restrict);

    entity.Property(e => e.BookingType).HasConversion<byte>();
    entity.Property(e => e.Status).HasConversion<byte>();

    // Gefilterter Unique-Index: max. 1 Running-Buchung pro WorkOperation
    entity.HasIndex(e => e.WorkOperationId)
        .IsUnique()
        .HasFilter("[EndedAt] IS NULL AND [IsCancelled] = 0 AND [WorkOperationId] IS NOT NULL")
        .HasDatabaseName("IX_BdeBookings_WorkOperationId_Active");

    // Gefilterter Unique-Index: max. 1 Running-Buchung pro Operator
    entity.HasIndex(e => e.BdeOperatorId)
        .IsUnique()
        .HasFilter("[EndedAt] IS NULL AND [IsCancelled] = 0")
        .HasDatabaseName("IX_BdeBookings_BdeOperatorId_Active");

    // Cockpit-Query
    entity.HasIndex(e => new { e.ProductionWorkplaceId, e.EndedAt })
        .HasDatabaseName("IX_BdeBookings_Workplace_EndedAt");

    // Operator-Historie
    entity.HasIndex(e => new { e.BdeOperatorId, e.StartedAt })
        .HasDatabaseName("IX_BdeBookings_Operator_StartedAt");

    // Allgemeine Sortierung
    entity.HasIndex(e => e.StartedAt)
        .HasDatabaseName("IX_BdeBookings_StartedAt");

    // Check-Constraint: genau eines von WorkOperationId / BdeActivityId gesetzt
    entity.ToTable(t => t.HasCheckConstraint("CK_BdeBookings_Target",
        "([WorkOperationId] IS NOT NULL AND [BdeActivityId] IS NULL) OR ([WorkOperationId] IS NULL AND [BdeActivityId] IS NOT NULL)"));

    // Check-Constraint: BookingType passt zu Ziel
    entity.ToTable(t => t.HasCheckConstraint("CK_BdeBookings_TypeTarget",
        "([BookingType] = 3 AND [BdeActivityId] IS NOT NULL) OR ([BookingType] IN (1,2) AND [WorkOperationId] IS NOT NULL)"));

    // Check-Constraint: Status-Konsistenz (Running ⇔ EndedAt NULL)
    entity.ToTable(t => t.HasCheckConstraint("CK_BdeBookings_StatusEnded",
        "([Status] = 1 AND [EndedAt] IS NULL) OR ([Status] IN (2,3) AND [EndedAt] IS NOT NULL)"));
});

// BDE: BookingQuantities
modelBuilder.Entity<BdeBookingQuantity>(entity =>
{
    entity.HasOne(e => e.BdeBooking).WithMany(b => b.Quantities).HasForeignKey(e => e.BdeBookingId).OnDelete(DeleteBehavior.Cascade);
    entity.HasOne(e => e.BdeOperator).WithMany().HasForeignKey(e => e.BdeOperatorId).OnDelete(DeleteBehavior.Restrict);

    entity.HasIndex(e => new { e.BdeBookingId, e.ReportedAt })
        .HasDatabaseName("IX_BdeBookingQuantities_Booking_ReportedAt");

    entity.HasIndex(e => e.BdeBookingId)
        .IsUnique()
        .HasFilter("[IsFinal] = 1")
        .HasDatabaseName("IX_BdeBookingQuantities_Booking_Final");
});
```

- [ ] **Step 3: Build**

```
dotnet build IdealAkeWms/IdealAkeWms.csproj
```

Expected: Build erfolgreich.

- [ ] **Step 4: Commit**

```bash
git add IdealAkeWms/Data/ApplicationDbContext.cs
git commit -m "feat(bde): configure BDE entities in DbContext"
```

---

## Task 5: EF Migration

**Files:**
- Create: `IdealAkeWms/Migrations/<timestamp>_AddBde.cs` (auto-generiert)

- [ ] **Step 1: Migration generieren**

```
dotnet ef migrations add AddBde --project IdealAkeWms --startup-project IdealAkeWms
```

Expected: Migration-Datei + Designer-Datei in `IdealAkeWms/Migrations/` erstellt, `ApplicationDbContextModelSnapshot.cs` aktualisiert.

- [ ] **Step 2: Migration-Inhalt prüfen**

Öffne die neue Migration und verifiziere:
- 5 `CreateTable`-Aufrufe (BdeTerminals, BdeOperators, BdeActivities, BdeBookings, BdeBookingQuantities)
- Alle 5 gefilterten Unique-Indizes vorhanden
- 3 Check-Constraints auf BdeBookings
- FK-Constraints korrekt

Falls EF etwas übersehen hat: Plan anhalten und Model-Konfig in Task 4 nachbessern.

- [ ] **Step 3: Build**

```
dotnet build IdealAkeWms/IdealAkeWms.csproj
```

- [ ] **Step 4: Commit**

```bash
git add IdealAkeWms/Migrations/
git commit -m "feat(bde): add EF migration for BDE entities"
```

---

## Task 6: SQL-Script 42_AddBde.sql

**Files:**
- Create: `SQL/42_AddBde.sql`

- [ ] **Step 1: SQL-Migration schreiben**

Datei `SQL/42_AddBde.sql`. Struktur: idempotent mit `OBJECT_ID`-Guards, Tabellen-Create in separatem Batch (GO) vor Index-/Constraint-Create, `__EFMigrationsHistory`-Insert am Ende in eigenem Batch:

```sql
-- =============================================
-- 42_AddBde.sql
-- Betriebsdatenerfassung (BDE) Phase 1
-- =============================================

-- 1) BdeTerminals
IF OBJECT_ID(N'dbo.BdeTerminals', N'U') IS NULL
BEGIN
    CREATE TABLE [dbo].[BdeTerminals] (
        [Id] INT IDENTITY(1,1) NOT NULL PRIMARY KEY,
        [UserId] INT NOT NULL,
        [DefaultProductionWorkplaceId] INT NOT NULL,
        [Description] NVARCHAR(200) NULL,
        [CreatedAt] DATETIME2 NOT NULL,
        [CreatedBy] NVARCHAR(200) NOT NULL,
        [CreatedByWindows] NVARCHAR(200) NOT NULL,
        [ModifiedAt] DATETIME2 NULL,
        [ModifiedBy] NVARCHAR(200) NULL,
        [ModifiedByWindows] NVARCHAR(200) NULL,
        CONSTRAINT [FK_BdeTerminals_Users] FOREIGN KEY ([UserId]) REFERENCES [dbo].[Users]([Id]),
        CONSTRAINT [FK_BdeTerminals_ProductionWorkplaces] FOREIGN KEY ([DefaultProductionWorkplaceId]) REFERENCES [dbo].[ProductionWorkplaces]([Id])
    );
END
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeTerminals_UserId' AND object_id = OBJECT_ID(N'dbo.BdeTerminals'))
    CREATE UNIQUE INDEX [IX_BdeTerminals_UserId] ON [dbo].[BdeTerminals]([UserId]);
GO

-- 2) BdeOperators
IF OBJECT_ID(N'dbo.BdeOperators', N'U') IS NULL
BEGIN
    CREATE TABLE [dbo].[BdeOperators] (
        [Id] INT IDENTITY(1,1) NOT NULL PRIMARY KEY,
        [PersonnelNumber] NVARCHAR(50) NOT NULL,
        [FirstName] NVARCHAR(100) NOT NULL,
        [LastName] NVARCHAR(100) NOT NULL,
        [IsActive] BIT NOT NULL DEFAULT 1,
        [UserId] INT NULL,
        [CreatedAt] DATETIME2 NOT NULL,
        [CreatedBy] NVARCHAR(200) NOT NULL,
        [CreatedByWindows] NVARCHAR(200) NOT NULL,
        [ModifiedAt] DATETIME2 NULL,
        [ModifiedBy] NVARCHAR(200) NULL,
        [ModifiedByWindows] NVARCHAR(200) NULL,
        CONSTRAINT [FK_BdeOperators_Users] FOREIGN KEY ([UserId]) REFERENCES [dbo].[Users]([Id]) ON DELETE SET NULL
    );
END
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeOperators_PersonnelNumber' AND object_id = OBJECT_ID(N'dbo.BdeOperators'))
    CREATE UNIQUE INDEX [IX_BdeOperators_PersonnelNumber] ON [dbo].[BdeOperators]([PersonnelNumber]);
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeOperators_UserId' AND object_id = OBJECT_ID(N'dbo.BdeOperators'))
    CREATE UNIQUE INDEX [IX_BdeOperators_UserId] ON [dbo].[BdeOperators]([UserId]) WHERE [UserId] IS NOT NULL;
GO

-- 3) BdeActivities
IF OBJECT_ID(N'dbo.BdeActivities', N'U') IS NULL
BEGIN
    CREATE TABLE [dbo].[BdeActivities] (
        [Id] INT IDENTITY(1,1) NOT NULL PRIMARY KEY,
        [Code] NVARCHAR(20) NOT NULL,
        [Name] NVARCHAR(100) NOT NULL,
        [IsActive] BIT NOT NULL DEFAULT 1,
        [CreatedAt] DATETIME2 NOT NULL,
        [CreatedBy] NVARCHAR(200) NOT NULL,
        [CreatedByWindows] NVARCHAR(200) NOT NULL,
        [ModifiedAt] DATETIME2 NULL,
        [ModifiedBy] NVARCHAR(200) NULL,
        [ModifiedByWindows] NVARCHAR(200) NULL
    );
END
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeActivities_Code' AND object_id = OBJECT_ID(N'dbo.BdeActivities'))
    CREATE UNIQUE INDEX [IX_BdeActivities_Code] ON [dbo].[BdeActivities]([Code]);
GO

-- 4) BdeBookings
IF OBJECT_ID(N'dbo.BdeBookings', N'U') IS NULL
BEGIN
    CREATE TABLE [dbo].[BdeBookings] (
        [Id] INT IDENTITY(1,1) NOT NULL PRIMARY KEY,
        [BdeOperatorId] INT NOT NULL,
        [ProductionWorkplaceId] INT NOT NULL,
        [BdeTerminalId] INT NOT NULL,
        [WorkOperationId] INT NULL,
        [BdeActivityId] INT NULL,
        [BookingType] TINYINT NOT NULL,
        [Status] TINYINT NOT NULL,
        [StartedAt] DATETIME2 NOT NULL,
        [EndedAt] DATETIME2 NULL,
        [IsCancelled] BIT NOT NULL DEFAULT 0,
        [CancellationReason] NVARCHAR(500) NULL,
        [ParentBookingId] INT NULL,
        [CreatedAt] DATETIME2 NOT NULL,
        [CreatedBy] NVARCHAR(200) NOT NULL,
        [CreatedByWindows] NVARCHAR(200) NOT NULL,
        [ModifiedAt] DATETIME2 NULL,
        [ModifiedBy] NVARCHAR(200) NULL,
        [ModifiedByWindows] NVARCHAR(200) NULL,
        CONSTRAINT [FK_BdeBookings_Operators] FOREIGN KEY ([BdeOperatorId]) REFERENCES [dbo].[BdeOperators]([Id]),
        CONSTRAINT [FK_BdeBookings_Workplaces] FOREIGN KEY ([ProductionWorkplaceId]) REFERENCES [dbo].[ProductionWorkplaces]([Id]),
        CONSTRAINT [FK_BdeBookings_Terminals] FOREIGN KEY ([BdeTerminalId]) REFERENCES [dbo].[BdeTerminals]([Id]),
        CONSTRAINT [FK_BdeBookings_WorkOperations] FOREIGN KEY ([WorkOperationId]) REFERENCES [dbo].[WorkOperations]([Id]),
        CONSTRAINT [FK_BdeBookings_Activities] FOREIGN KEY ([BdeActivityId]) REFERENCES [dbo].[BdeActivities]([Id]),
        CONSTRAINT [FK_BdeBookings_Parent] FOREIGN KEY ([ParentBookingId]) REFERENCES [dbo].[BdeBookings]([Id]),
        CONSTRAINT [CK_BdeBookings_Target] CHECK (
            ([WorkOperationId] IS NOT NULL AND [BdeActivityId] IS NULL) OR
            ([WorkOperationId] IS NULL AND [BdeActivityId] IS NOT NULL)
        ),
        CONSTRAINT [CK_BdeBookings_TypeTarget] CHECK (
            ([BookingType] = 3 AND [BdeActivityId] IS NOT NULL) OR
            ([BookingType] IN (1,2) AND [WorkOperationId] IS NOT NULL)
        ),
        CONSTRAINT [CK_BdeBookings_StatusEnded] CHECK (
            ([Status] = 1 AND [EndedAt] IS NULL) OR
            ([Status] IN (2,3) AND [EndedAt] IS NOT NULL)
        )
    );
END
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeBookings_WorkOperationId_Active' AND object_id = OBJECT_ID(N'dbo.BdeBookings'))
    CREATE UNIQUE INDEX [IX_BdeBookings_WorkOperationId_Active]
        ON [dbo].[BdeBookings]([WorkOperationId])
        WHERE [EndedAt] IS NULL AND [IsCancelled] = 0 AND [WorkOperationId] IS NOT NULL;
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeBookings_BdeOperatorId_Active' AND object_id = OBJECT_ID(N'dbo.BdeBookings'))
    CREATE UNIQUE INDEX [IX_BdeBookings_BdeOperatorId_Active]
        ON [dbo].[BdeBookings]([BdeOperatorId])
        WHERE [EndedAt] IS NULL AND [IsCancelled] = 0;
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeBookings_Workplace_EndedAt' AND object_id = OBJECT_ID(N'dbo.BdeBookings'))
    CREATE INDEX [IX_BdeBookings_Workplace_EndedAt] ON [dbo].[BdeBookings]([ProductionWorkplaceId],[EndedAt]);
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeBookings_Operator_StartedAt' AND object_id = OBJECT_ID(N'dbo.BdeBookings'))
    CREATE INDEX [IX_BdeBookings_Operator_StartedAt] ON [dbo].[BdeBookings]([BdeOperatorId],[StartedAt]);
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeBookings_StartedAt' AND object_id = OBJECT_ID(N'dbo.BdeBookings'))
    CREATE INDEX [IX_BdeBookings_StartedAt] ON [dbo].[BdeBookings]([StartedAt]);
GO

-- 5) BdeBookingQuantities
IF OBJECT_ID(N'dbo.BdeBookingQuantities', N'U') IS NULL
BEGIN
    CREATE TABLE [dbo].[BdeBookingQuantities] (
        [Id] INT IDENTITY(1,1) NOT NULL PRIMARY KEY,
        [BdeBookingId] INT NOT NULL,
        [BdeOperatorId] INT NOT NULL,
        [GoodQuantity] DECIMAL(18,4) NOT NULL DEFAULT 0,
        [ScrapQuantity] DECIMAL(18,4) NOT NULL DEFAULT 0,
        [IsFinal] BIT NOT NULL,
        [ReportedAt] DATETIME2 NOT NULL,
        [CreatedAt] DATETIME2 NOT NULL,
        [CreatedBy] NVARCHAR(200) NOT NULL,
        [CreatedByWindows] NVARCHAR(200) NOT NULL,
        [ModifiedAt] DATETIME2 NULL,
        [ModifiedBy] NVARCHAR(200) NULL,
        [ModifiedByWindows] NVARCHAR(200) NULL,
        CONSTRAINT [FK_BdeBookingQuantities_Bookings] FOREIGN KEY ([BdeBookingId]) REFERENCES [dbo].[BdeBookings]([Id]) ON DELETE CASCADE,
        CONSTRAINT [FK_BdeBookingQuantities_Operators] FOREIGN KEY ([BdeOperatorId]) REFERENCES [dbo].[BdeOperators]([Id])
    );
END
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeBookingQuantities_Booking_ReportedAt' AND object_id = OBJECT_ID(N'dbo.BdeBookingQuantities'))
    CREATE INDEX [IX_BdeBookingQuantities_Booking_ReportedAt] ON [dbo].[BdeBookingQuantities]([BdeBookingId],[ReportedAt]);
GO

IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_BdeBookingQuantities_Booking_Final' AND object_id = OBJECT_ID(N'dbo.BdeBookingQuantities'))
    CREATE UNIQUE INDEX [IX_BdeBookingQuantities_Booking_Final] ON [dbo].[BdeBookingQuantities]([BdeBookingId]) WHERE [IsFinal] = 1;
GO

-- 6) Rollen-Seed
IF NOT EXISTS (SELECT 1 FROM [dbo].[Roles] WHERE [Key] = 'bde_user')
    INSERT INTO [dbo].[Roles] ([Key],[DisplayName],[AdGroup],[CreatedAt],[CreatedBy],[CreatedByWindows])
    VALUES ('bde_user','BDE-Mitarbeiter',NULL,SYSUTCDATETIME(),'System','System');
GO

IF NOT EXISTS (SELECT 1 FROM [dbo].[Roles] WHERE [Key] = 'bde_shiftlead')
    INSERT INTO [dbo].[Roles] ([Key],[DisplayName],[AdGroup],[CreatedAt],[CreatedBy],[CreatedByWindows])
    VALUES ('bde_shiftlead','BDE-Schichtleiter',NULL,SYSUTCDATETIME(),'System','System');
GO

IF NOT EXISTS (SELECT 1 FROM [dbo].[Roles] WHERE [Key] = 'bde_admin')
    INSERT INTO [dbo].[Roles] ([Key],[DisplayName],[AdGroup],[CreatedAt],[CreatedBy],[CreatedByWindows])
    VALUES ('bde_admin','BDE-Admin',NULL,SYSUTCDATETIME(),'System','System');
GO

-- 7) __EFMigrationsHistory
IF NOT EXISTS (SELECT 1 FROM [dbo].[__EFMigrationsHistory] WHERE [MigrationId] LIKE '%_AddBde')
BEGIN
    DECLARE @MigrationId NVARCHAR(150);
    SELECT TOP 1 @MigrationId = [MigrationId]
    FROM (VALUES (CONVERT(NVARCHAR(14), SYSUTCDATETIME(), 112) + '000000_AddBde')) AS V([MigrationId]);
    -- Produktive Installation: exakten MigrationId-Wert aus der generierten Migration-Datei einsetzen
END
GO
```

**WICHTIG:** Der `__EFMigrationsHistory`-Insert muss den **exakten** `MigrationId`-Wert aus der in Task 5 generierten Migration-Datei verwenden (z.B. `20260414123456_AddBde`). Den Platzhalter in obigem Block ersetzen durch:

```sql
IF NOT EXISTS (SELECT 1 FROM [dbo].[__EFMigrationsHistory] WHERE [MigrationId] = '<exakter-migrationId>')
    INSERT INTO [dbo].[__EFMigrationsHistory] ([MigrationId],[ProductVersion])
    VALUES ('<exakter-migrationId>','10.0.0');
GO
```

- [ ] **Step 2: Commit**

```bash
git add SQL/42_AddBde.sql
git commit -m "feat(bde): add SQL migration for BDE phase 1"
```

---

## Task 7: 00_FreshInstall.sql aktualisieren

**Files:**
- Modify: `SQL/00_FreshInstall.sql`

- [ ] **Step 1: BDE-Block einfügen**

Den kompletten Inhalt von `SQL/42_AddBde.sql` (ohne `__EFMigrationsHistory`-Insert am Ende) in `SQL/00_FreshInstall.sql` vor dem letzten GO-Block einfügen. Anschließend den Roles-Seed so anpassen, dass er zusammen mit den anderen Rollen-Seeds in einem Block steht.

Zusätzlich den `__EFMigrationsHistory`-Insert für `_AddBde` am Ende der Datei ergänzen (zusammen mit den anderen History-Einträgen).

- [ ] **Step 2: Commit**

```bash
git add SQL/00_FreshInstall.sql
git commit -m "feat(bde): update FreshInstall script with BDE tables and roles"
```

---

## Task 8: CurrentUserService erweitern

**Files:**
- Modify: `IdealAkeWms/Services/ICurrentUserService.cs`
- Modify: `IdealAkeWms/Services/CurrentUserService.cs`

- [ ] **Step 1: Interface-Methoden ergänzen**

In `ICurrentUserService.cs` am Ende ergänzen:

```csharp
Task<bool> CanUseBdeAsync();
Task<bool> CanManageBdeShiftleadAsync();
Task<bool> CanManageBdeAdminAsync();
```

- [ ] **Step 2: Implementierungen in `CurrentUserService`**

Analog zu den bestehenden Implementierungen (z.B. `CanManagePickingReleaseAsync`):

```csharp
public Task<bool> CanUseBdeAsync()
    => HasAnyRoleAsync(RoleKeys.Admin, RoleKeys.BdeUser, RoleKeys.BdeShiftlead, RoleKeys.BdeAdmin);

public Task<bool> CanManageBdeShiftleadAsync()
    => HasAnyRoleAsync(RoleKeys.Admin, RoleKeys.BdeShiftlead, RoleKeys.BdeAdmin);

public Task<bool> CanManageBdeAdminAsync()
    => HasAnyRoleAsync(RoleKeys.Admin, RoleKeys.BdeAdmin);
```

- [ ] **Step 3: Build + Commit**

```bash
dotnet build IdealAkeWms/IdealAkeWms.csproj
git add IdealAkeWms/Services/
git commit -m "feat(bde): add current user service checks for BDE roles"
```

---

## Task 9: Filter-Attribute

**Files:**
- Create: `IdealAkeWms/Filters/RequireBdeUserAccessAttribute.cs`
- Create: `IdealAkeWms/Filters/RequireBdeShiftleadAccessAttribute.cs`
- Create: `IdealAkeWms/Filters/RequireBdeAdminAccessAttribute.cs`

- [ ] **Step 1: `RequireBdeUserAccessAttribute`**

Datei `IdealAkeWms/Filters/RequireBdeUserAccessAttribute.cs`:

```csharp
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.Filters;
using IdealAkeWms.Services;

namespace IdealAkeWms.Filters;

public class RequireBdeUserAccessAttribute : TypeFilterAttribute
{
    public RequireBdeUserAccessAttribute() : base(typeof(RequireBdeUserAccessFilter)) { }
}

public class RequireBdeUserAccessFilter : IAsyncActionFilter
{
    private readonly ICurrentUserService _currentUserService;

    public RequireBdeUserAccessFilter(ICurrentUserService currentUserService)
    {
        _currentUserService = currentUserService;
    }

    public async Task OnActionExecutionAsync(ActionExecutingContext context, ActionExecutionDelegate next)
    {
        if (!await _currentUserService.CanUseBdeAsync())
        {
            context.Result = new RedirectToActionResult("AccessDenied", "Account", null);
            return;
        }
        await next();
    }
}
```

- [ ] **Step 2: `RequireBdeShiftleadAccessAttribute`** und **Step 3: `RequireBdeAdminAccessAttribute`**

Analog zu Step 1, nur mit aufrufendem Check `CanManageBdeShiftleadAsync()` bzw. `CanManageBdeAdminAsync()` und entsprechend umbenannten Klassen.

- [ ] **Step 4: Build + Commit**

```bash
dotnet build IdealAkeWms/IdealAkeWms.csproj
git add IdealAkeWms/Filters/
git commit -m "feat(bde): add BDE role filter attributes"
```

---

## Task 10: BdeOperatorRepository (TDD)

**Files:**
- Create: `IdealAkeWms/Data/Repositories/IBdeOperatorRepository.cs`
- Create: `IdealAkeWms/Data/Repositories/BdeOperatorRepository.cs`
- Create: `IdealAkeWms.Tests/Repositories/BdeOperatorRepositoryTests.cs`

- [ ] **Step 1: Tests schreiben (failing)**

Datei `IdealAkeWms.Tests/Repositories/BdeOperatorRepositoryTests.cs`:

```csharp
using FluentAssertions;
using IdealAkeWms.Data.Repositories;
using IdealAkeWms.Models;
using IdealAkeWms.Tests.Helpers;
using Xunit;

namespace IdealAkeWms.Tests.Repositories;

public class BdeOperatorRepositoryTests
{
    [Fact]
    public async Task GetByPersonnelNumber_ReturnsOperator_WhenExists()
    {
        using var ctx = TestDbContextFactory.Create();
        ctx.BdeOperators.Add(new BdeOperator {
            PersonnelNumber = "P123", FirstName = "Max", LastName = "Muster",
            IsActive = true,
            CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t"
        });
        await ctx.SaveChangesAsync();

        var repo = new BdeOperatorRepository(ctx);
        var op = await repo.GetByPersonnelNumberAsync("P123");

        op.Should().NotBeNull();
        op!.FirstName.Should().Be("Max");
    }

    [Fact]
    public async Task GetByPersonnelNumber_ReturnsNull_WhenInactive()
    {
        using var ctx = TestDbContextFactory.Create();
        ctx.BdeOperators.Add(new BdeOperator {
            PersonnelNumber = "P999", FirstName = "In", LastName = "Aktiv",
            IsActive = false,
            CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t"
        });
        await ctx.SaveChangesAsync();

        var repo = new BdeOperatorRepository(ctx);
        var op = await repo.GetByPersonnelNumberAsync("P999");

        op.Should().BeNull();
    }

    [Fact]
    public async Task GetAllActive_OrdersByLastName()
    {
        using var ctx = TestDbContextFactory.Create();
        ctx.BdeOperators.AddRange(
            new BdeOperator { PersonnelNumber = "2", FirstName = "A", LastName = "Zeta", IsActive = true, CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t" },
            new BdeOperator { PersonnelNumber = "1", FirstName = "A", LastName = "Alpha", IsActive = true, CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t" }
        );
        await ctx.SaveChangesAsync();

        var repo = new BdeOperatorRepository(ctx);
        var list = await repo.GetAllActiveAsync();

        list.Select(o => o.LastName).Should().ContainInOrder("Alpha", "Zeta");
    }
}
```

- [ ] **Step 2: Tests ausführen (rot)**

```
dotnet test IdealAkeWms.Tests --filter FullyQualifiedName~BdeOperatorRepositoryTests
```

Expected: Build fails, "IBdeOperatorRepository not found" — erwartet.

- [ ] **Step 3: Interface + Implementierung**

Datei `IdealAkeWms/Data/Repositories/IBdeOperatorRepository.cs`:

```csharp
using IdealAkeWms.Models;

namespace IdealAkeWms.Data.Repositories;

public interface IBdeOperatorRepository
{
    Task<List<BdeOperator>> GetAllAsync();
    Task<List<BdeOperator>> GetAllActiveAsync();
    Task<BdeOperator?> GetByIdAsync(int id);
    Task<BdeOperator?> GetByPersonnelNumberAsync(string personnelNumber);
    Task AddAsync(BdeOperator op);
    Task UpdateAsync(BdeOperator op);
}
```

Datei `IdealAkeWms/Data/Repositories/BdeOperatorRepository.cs`:

```csharp
using IdealAkeWms.Data;
using IdealAkeWms.Models;
using Microsoft.EntityFrameworkCore;

namespace IdealAkeWms.Data.Repositories;

public class BdeOperatorRepository : IBdeOperatorRepository
{
    private readonly ApplicationDbContext _ctx;
    public BdeOperatorRepository(ApplicationDbContext ctx) { _ctx = ctx; }

    public Task<List<BdeOperator>> GetAllAsync() =>
        _ctx.BdeOperators.OrderBy(o => o.LastName).ThenBy(o => o.FirstName).ToListAsync();

    public Task<List<BdeOperator>> GetAllActiveAsync() =>
        _ctx.BdeOperators.Where(o => o.IsActive).OrderBy(o => o.LastName).ThenBy(o => o.FirstName).ToListAsync();

    public Task<BdeOperator?> GetByIdAsync(int id) =>
        _ctx.BdeOperators.FirstOrDefaultAsync(o => o.Id == id);

    public Task<BdeOperator?> GetByPersonnelNumberAsync(string personnelNumber) =>
        _ctx.BdeOperators.FirstOrDefaultAsync(o => o.PersonnelNumber == personnelNumber && o.IsActive);

    public async Task AddAsync(BdeOperator op)
    {
        _ctx.BdeOperators.Add(op);
        await _ctx.SaveChangesAsync();
    }

    public async Task UpdateAsync(BdeOperator op)
    {
        _ctx.BdeOperators.Update(op);
        await _ctx.SaveChangesAsync();
    }
}
```

- [ ] **Step 4: Tests ausführen (grün)**

```
dotnet test IdealAkeWms.Tests --filter FullyQualifiedName~BdeOperatorRepositoryTests
```

Expected: 3 passed.

- [ ] **Step 5: DI registrieren**

In `Program.cs` bei den Repository-Registrierungen ergänzen:

```csharp
builder.Services.AddScoped<IBdeOperatorRepository, BdeOperatorRepository>();
```

- [ ] **Step 6: Commit**

```bash
git add IdealAkeWms/Data/Repositories/IBdeOperatorRepository.cs IdealAkeWms/Data/Repositories/BdeOperatorRepository.cs IdealAkeWms.Tests/Repositories/BdeOperatorRepositoryTests.cs IdealAkeWms/Program.cs
git commit -m "feat(bde): add BdeOperatorRepository with tests"
```

---

## Task 11: BdeActivityRepository (TDD)

**Files:**
- Create: `IdealAkeWms/Data/Repositories/IBdeActivityRepository.cs`
- Create: `IdealAkeWms/Data/Repositories/BdeActivityRepository.cs`
- Create: `IdealAkeWms.Tests/Repositories/BdeActivityRepositoryTests.cs`

Analog zu Task 10. Repository-Methoden:
- `GetAllAsync()`, `GetAllActiveAsync()`, `GetByIdAsync(int)`, `GetByCodeAsync(string)`, `AddAsync`, `UpdateAsync`.

Tests:
- `GetAllActive_FiltersInactive`
- `GetByCode_ReturnsActivity`
- `GetByCode_IsCaseSensitive` (entscheide bewusst: Code "WART" ≠ "wart")

Code folgt dem Pattern von Task 10. DI-Registrierung in `Program.cs`. Tests müssen grün laufen bevor commit.

Commit: `git commit -m "feat(bde): add BdeActivityRepository with tests"`

---

## Task 12: BdeTerminalRepository (TDD)

**Files:**
- Create: `IdealAkeWms/Data/Repositories/IBdeTerminalRepository.cs`
- Create: `IdealAkeWms/Data/Repositories/BdeTerminalRepository.cs`
- Create: `IdealAkeWms.Tests/Repositories/BdeTerminalRepositoryTests.cs`

Repository-Methoden:
- `GetAllAsync()` (inkl. User und DefaultProductionWorkplace via `Include`)
- `GetByIdAsync(int)`
- `GetByUserIdAsync(int)` — für Terminal-Ermittlung des eingeloggten Users
- `AddAsync`, `UpdateAsync`, `DeleteAsync`

Tests:
- `GetByUserId_ReturnsTerminal_WithWorkplaceIncluded`
- `GetByUserId_ReturnsNull_WhenUserHasNoTerminal`

Dafür muss ein `User` + `ProductionWorkplace` als Test-Setup angelegt werden.

DI in `Program.cs`. Commit analog.

---

## Task 13: BdeBookingRepository (TDD, Kern)

**Files:**
- Create: `IdealAkeWms/Data/Repositories/IBdeBookingRepository.cs`
- Create: `IdealAkeWms/Data/Repositories/BdeBookingRepository.cs`
- Create: `IdealAkeWms.Tests/Repositories/BdeBookingRepositoryTests.cs`

Repository-Methoden (reine Datenschicht — **keine** State-Machine-Logik, die kommt in Task 14):

```csharp
public interface IBdeBookingRepository
{
    Task<BdeBooking?> GetByIdAsync(int id);
    Task<BdeBooking?> GetActiveForOperatorAsync(int operatorId);
    Task<BdeBooking?> GetActiveForWorkOperationAsync(int workOperationId);
    Task<BdeBooking?> GetLatestPausedForWorkOperationAsync(int workOperationId);
    Task<List<BdeBooking>> GetActiveCockpitAsync();
    Task<List<BdeBooking>> GetHistoryAsync(int skip, int take, int? operatorId, int? workplaceId, DateTime? from, DateTime? to);
    Task AddAsync(BdeBooking booking);
    Task UpdateAsync(BdeBooking booking);
    Task<decimal> GetTotalGoodAsync(int bookingId);
    Task<decimal> GetTotalScrapAsync(int bookingId);
}
```

- [ ] **Step 1: Tests schreiben**

Tests, die die Queries absichern:
- `GetActiveForOperator_ReturnsRunning`
- `GetActiveForOperator_IgnoresCancelled`
- `GetActiveForOperator_IgnoresFinished`
- `GetActiveForWorkOperation_ReturnsRunning`
- `GetLatestPausedForWorkOperation_ReturnsMostRecent`
- `GetActiveCockpit_IncludesWorkOperationAndOperator` (Prüft `Include`-Graph)
- `GetTotalGood_SumsAllQuantities_IgnoringCancelled` (Storno auf Parent-Booking muss Quantity ausblenden — falls Storno-Logik kaskadiert; hier: nur `WHERE BdeBookingId = x`, Cancel-Filter ist Aufruferaufgabe)

Alle Tests mit ausreichendem Setup (Operator, Workplace, Terminal, WorkOperation oder Activity). Verwende einen kleinen Test-Helper `BdeTestData.Seed(ctx)` der Basic-Entities anlegt — nicht auf alle Tests verteilen.

- [ ] **Step 2: Implementierung**

Repository mit den oben genannten Methoden. `GetActiveCockpitAsync` muss `.Include(b => b.BdeOperator).Include(b => b.ProductionWorkplace).Include(b => b.WorkOperation).ThenInclude(w => w!.ProductionOrder).Include(b => b.BdeActivity)` enthalten.

- [ ] **Step 3: Tests grün, DI registrieren, Commit**

---

## Task 14: BdeBookingService (State-Machine, TDD) — Kern-Logik

**Files:**
- Create: `IdealAkeWms/Services/IBdeBookingService.cs`
- Create: `IdealAkeWms/Services/BdeBookingService.cs`
- Create: `IdealAkeWms/Services/BdeBookingResult.cs`
- Create: `IdealAkeWms.Tests/Services/BdeBookingServiceTests.cs`

Das ist der kritischste Task. Die State-Machine wird hier gekapselt, damit Controller einfache Aufrufe machen können und Tests die Regeln absichern.

- [ ] **Step 1: Result-Typ definieren**

Datei `IdealAkeWms/Services/BdeBookingResult.cs`:

```csharp
using IdealAkeWms.Models;

namespace IdealAkeWms.Services;

public enum BdeBookingOutcome
{
    Success,
    CollisionOtherOperator,
    QuantityRequired,
    InvalidState,
    NotFound
}

public class BdeBookingResult
{
    public BdeBookingOutcome Outcome { get; init; }
    public BdeBooking? Booking { get; init; }
    public BdeBooking? CollidingBooking { get; init; }
    public string? Message { get; init; }

    public static BdeBookingResult Success(BdeBooking b) => new() { Outcome = BdeBookingOutcome.Success, Booking = b };
    public static BdeBookingResult Collision(BdeBooking colliding) => new() { Outcome = BdeBookingOutcome.CollisionOtherOperator, CollidingBooking = colliding };
    public static BdeBookingResult QuantityRequired(BdeBooking current) => new() { Outcome = BdeBookingOutcome.QuantityRequired, Booking = current };
    public static BdeBookingResult Invalid(string msg) => new() { Outcome = BdeBookingOutcome.InvalidState, Message = msg };
    public static BdeBookingResult NotFound() => new() { Outcome = BdeBookingOutcome.NotFound };
}
```

- [ ] **Step 2: Interface**

Datei `IdealAkeWms/Services/IBdeBookingService.cs`:

```csharp
using IdealAkeWms.Models;

namespace IdealAkeWms.Services;

public interface IBdeBookingService
{
    Task<BdeBookingResult> StartSetupAsync(int operatorId, int workOperationId, int workplaceId, int terminalId);
    Task<BdeBookingResult> StartProductionAsync(int operatorId, int workOperationId, int workplaceId, int terminalId);
    Task<BdeBookingResult> StartActivityAsync(int operatorId, int activityId, int workplaceId, int terminalId);
    Task<BdeBookingResult> PauseAsync(int bookingId, decimal? goodQty = null, decimal? scrapQty = null);
    Task<BdeBookingResult> ResumeAsync(int pausedBookingId, int operatorId, BdeBookingType resumeAs, int workplaceId, int terminalId);
    Task<BdeBookingResult> FinishAsync(int bookingId, decimal? goodQty = null, decimal? scrapQty = null);
    Task<BdeBookingResult> ReportPartialQuantityAsync(int bookingId, decimal goodQty, decimal scrapQty);
}
```

- [ ] **Step 3: Tests (State-Machine-Regeln)**

Datei `IdealAkeWms.Tests/Services/BdeBookingServiceTests.cs`. Jeder Test baut Setup via Helper `SeedBasics(ctx, out var opId, out var woId, out var wpId, out var tId)`.

Kern-Tests (jeder als eigener `[Fact]`):

1. `StartSetup_CreatesRunningBooking_WithTypeSetup`
2. `StartSetup_WhenOperatorHasOtherSetup_AutoClosesOld`
3. `StartSetup_WhenOperatorHasOpenProduction_ReturnsQuantityRequired`
4. `StartSetup_WhenWorkOperationAlreadyActiveElsewhere_ReturnsCollision`
5. `StartProduction_FromSetupSameAG_AutoClosesSetupCreatesProduction`
6. `StartProduction_FromSetupSameAGDifferentOperator_ReturnsInvalidState` (Handover via Resume, nicht implizit)
7. `StartActivity_CreatesActivityBooking`
8. `Pause_Production_WithPartialQuantity_CreatesQuantityEntry_SetsStatusPaused`
9. `Pause_Setup_SetsStatusPaused_NoQuantity`
10. `Resume_SameOperator_CreatesNewBookingWithParentId`
11. `Resume_DifferentOperator_CreatesNewBookingWithParentIdAndNewOperator`
12. `Resume_FromPausedSetup_AsProduction_TransitionsType`
13. `Finish_Production_RequiresFinalQuantity_StoresIt`
14. `Finish_Setup_WithoutQuantity_Succeeds`
15. `Finish_Activity_WithoutQuantity_Succeeds`
16. `ReportPartialQuantity_AppendsNonFinalQuantity_KeepsStatusRunning`
17. `ReportPartialQuantity_OnFinishedBooking_ReturnsInvalidState`
18. `Collision_ShowsOtherOperatorAndWorkplace_InResult`

Für einen dieser Tests ausgeschrieben, Rest analog:

```csharp
[Fact]
public async Task StartSetup_WhenOperatorHasOpenProduction_ReturnsQuantityRequired()
{
    using var ctx = TestDbContextFactory.Create();
    BdeTestData.Seed(ctx, out var op1, out var wo1, out var wp1, out var term1);
    BdeTestData.SeedSecondWorkOperation(ctx, out var wo2);

    var svc = new BdeBookingService(ctx, new FakeCurrentUserService());
    // Operator 1 hat offene Production auf WO1
    await svc.StartProductionAsync(op1, wo1, wp1, term1);

    // Versucht Setup auf WO2 → QuantityRequired
    var result = await svc.StartSetupAsync(op1, wo2, wp1, term1);

    result.Outcome.Should().Be(BdeBookingOutcome.QuantityRequired);
    result.Booking!.WorkOperationId.Should().Be(wo1); // Aktuelle offene Buchung zurück
}
```

- [ ] **Step 4: Implementierung**

Datei `IdealAkeWms/Services/BdeBookingService.cs`. Kern-Skelett:

```csharp
using IdealAkeWms.Data;
using IdealAkeWms.Models;
using Microsoft.EntityFrameworkCore;

namespace IdealAkeWms.Services;

public class BdeBookingService : IBdeBookingService
{
    private readonly ApplicationDbContext _ctx;
    private readonly ICurrentUserService _userSvc;

    public BdeBookingService(ApplicationDbContext ctx, ICurrentUserService userSvc)
    {
        _ctx = ctx;
        _userSvc = userSvc;
    }

    public async Task<BdeBookingResult> StartSetupAsync(int operatorId, int workOperationId, int workplaceId, int terminalId)
        => await StartPlannedAsync(operatorId, workOperationId, workplaceId, terminalId, BdeBookingType.Setup);

    public async Task<BdeBookingResult> StartProductionAsync(int operatorId, int workOperationId, int workplaceId, int terminalId)
        => await StartPlannedAsync(operatorId, workOperationId, workplaceId, terminalId, BdeBookingType.Production);

    private async Task<BdeBookingResult> StartPlannedAsync(int operatorId, int workOperationId, int workplaceId, int terminalId, BdeBookingType type)
    {
        // 1) Kollision: läuft WorkOperation bereits bei anderem Operator?
        var existingOnWo = await _ctx.BdeBookings
            .Include(b => b.BdeOperator).Include(b => b.ProductionWorkplace)
            .FirstOrDefaultAsync(b => b.WorkOperationId == workOperationId && b.EndedAt == null && !b.IsCancelled);

        if (existingOnWo != null && existingOnWo.BdeOperatorId != operatorId)
            return BdeBookingResult.Collision(existingOnWo);

        // 2) Auto-Close der eigenen offenen Buchung?
        var existingOwn = await _ctx.BdeBookings
            .FirstOrDefaultAsync(b => b.BdeOperatorId == operatorId && b.EndedAt == null && !b.IsCancelled);

        if (existingOwn != null)
        {
            // Wenn es die GLEICHE WorkOperation ist und aktueller Typ=Setup + neuer=Production:
            // automatischer Übergang (bleibt beim gleichen Operator)
            if (existingOwn.WorkOperationId == workOperationId
                && existingOwn.BookingType == BdeBookingType.Setup
                && type == BdeBookingType.Production)
            {
                await FinishInternalAsync(existingOwn, null, null);
                return await CreatePlannedAsync(operatorId, workOperationId, workplaceId, terminalId, type, parentId: existingOwn.Id);
            }

            // Wechsel auf anderen AG
            if (existingOwn.BookingType == BdeBookingType.Production)
                return BdeBookingResult.QuantityRequired(existingOwn);

            // Setup oder Activity → einfach schließen
            await FinishInternalAsync(existingOwn, null, null);
        }

        return await CreatePlannedAsync(operatorId, workOperationId, workplaceId, terminalId, type, parentId: null);
    }

    public async Task<BdeBookingResult> StartActivityAsync(int operatorId, int activityId, int workplaceId, int terminalId)
    {
        var existingOwn = await _ctx.BdeBookings
            .FirstOrDefaultAsync(b => b.BdeOperatorId == operatorId && b.EndedAt == null && !b.IsCancelled);

        if (existingOwn != null)
        {
            if (existingOwn.BookingType == BdeBookingType.Production)
                return BdeBookingResult.QuantityRequired(existingOwn);
            await FinishInternalAsync(existingOwn, null, null);
        }

        var now = DateTime.Now;
        var booking = new BdeBooking
        {
            BdeOperatorId = operatorId,
            BdeActivityId = activityId,
            ProductionWorkplaceId = workplaceId,
            BdeTerminalId = terminalId,
            BookingType = BdeBookingType.Activity,
            Status = BdeBookingStatus.Running,
            StartedAt = now
        };
        SetAudit(booking);
        _ctx.BdeBookings.Add(booking);
        await _ctx.SaveChangesAsync();
        return BdeBookingResult.Success(booking);
    }

    public async Task<BdeBookingResult> PauseAsync(int bookingId, decimal? goodQty = null, decimal? scrapQty = null)
    {
        var b = await _ctx.BdeBookings.FindAsync(bookingId);
        if (b == null) return BdeBookingResult.NotFound();
        if (b.Status != BdeBookingStatus.Running)
            return BdeBookingResult.Invalid("Buchung ist nicht aktiv.");

        b.Status = BdeBookingStatus.Paused;
        b.EndedAt = DateTime.Now;
        SetAuditModified(b);

        if (b.BookingType == BdeBookingType.Production && (goodQty.HasValue || scrapQty.HasValue))
        {
            _ctx.BdeBookingQuantities.Add(CreatePartialQuantity(b, goodQty ?? 0, scrapQty ?? 0));
        }

        await _ctx.SaveChangesAsync();
        return BdeBookingResult.Success(b);
    }

    public async Task<BdeBookingResult> ResumeAsync(int pausedBookingId, int operatorId, BdeBookingType resumeAs, int workplaceId, int terminalId)
    {
        var parent = await _ctx.BdeBookings.FindAsync(pausedBookingId);
        if (parent == null) return BdeBookingResult.NotFound();
        if (parent.Status != BdeBookingStatus.Paused)
            return BdeBookingResult.Invalid("Ziel-Buchung ist nicht pausiert.");

        // Neue Running-Buchung mit ParentId erzeugen
        if (parent.WorkOperationId.HasValue)
        {
            // Kollision: inzwischen anderer Running?
            var collision = await _ctx.BdeBookings
                .Include(b => b.BdeOperator).Include(b => b.ProductionWorkplace)
                .FirstOrDefaultAsync(b => b.WorkOperationId == parent.WorkOperationId && b.EndedAt == null && !b.IsCancelled);
            if (collision != null)
                return BdeBookingResult.Collision(collision);

            // Operator-eigene offene → Auto-Close / QuantityRequired
            var existingOwn = await _ctx.BdeBookings
                .FirstOrDefaultAsync(b => b.BdeOperatorId == operatorId && b.EndedAt == null && !b.IsCancelled);
            if (existingOwn != null)
            {
                if (existingOwn.BookingType == BdeBookingType.Production)
                    return BdeBookingResult.QuantityRequired(existingOwn);
                await FinishInternalAsync(existingOwn, null, null);
            }
        }

        var now = DateTime.Now;
        var newBooking = new BdeBooking
        {
            BdeOperatorId = operatorId,
            ProductionWorkplaceId = workplaceId,
            BdeTerminalId = terminalId,
            WorkOperationId = parent.WorkOperationId,
            BdeActivityId = parent.BdeActivityId,
            BookingType = resumeAs,
            Status = BdeBookingStatus.Running,
            StartedAt = now,
            ParentBookingId = parent.Id
        };
        SetAudit(newBooking);
        _ctx.BdeBookings.Add(newBooking);
        await _ctx.SaveChangesAsync();
        return BdeBookingResult.Success(newBooking);
    }

    public async Task<BdeBookingResult> FinishAsync(int bookingId, decimal? goodQty = null, decimal? scrapQty = null)
    {
        var b = await _ctx.BdeBookings.FindAsync(bookingId);
        if (b == null) return BdeBookingResult.NotFound();
        if (b.Status == BdeBookingStatus.Finished)
            return BdeBookingResult.Invalid("Bereits beendet.");

        if (b.BookingType == BdeBookingType.Production && !goodQty.HasValue && !scrapQty.HasValue)
            return BdeBookingResult.QuantityRequired(b);

        await FinishInternalAsync(b, goodQty, scrapQty);
        await _ctx.SaveChangesAsync();
        return BdeBookingResult.Success(b);
    }

    public async Task<BdeBookingResult> ReportPartialQuantityAsync(int bookingId, decimal goodQty, decimal scrapQty)
    {
        var b = await _ctx.BdeBookings.FindAsync(bookingId);
        if (b == null) return BdeBookingResult.NotFound();
        if (b.Status != BdeBookingStatus.Running || b.BookingType != BdeBookingType.Production)
            return BdeBookingResult.Invalid("Teilmengen nur auf laufender Production.");

        _ctx.BdeBookingQuantities.Add(CreatePartialQuantity(b, goodQty, scrapQty));
        await _ctx.SaveChangesAsync();
        return BdeBookingResult.Success(b);
    }

    // --- Interne Helfer ---

    private async Task<BdeBookingResult> CreatePlannedAsync(int operatorId, int workOperationId, int workplaceId, int terminalId, BdeBookingType type, int? parentId)
    {
        var now = DateTime.Now;
        var booking = new BdeBooking
        {
            BdeOperatorId = operatorId,
            WorkOperationId = workOperationId,
            ProductionWorkplaceId = workplaceId,
            BdeTerminalId = terminalId,
            BookingType = type,
            Status = BdeBookingStatus.Running,
            StartedAt = now,
            ParentBookingId = parentId
        };
        SetAudit(booking);
        _ctx.BdeBookings.Add(booking);
        await _ctx.SaveChangesAsync();
        return BdeBookingResult.Success(booking);
    }

    private async Task FinishInternalAsync(BdeBooking b, decimal? goodQty, decimal? scrapQty)
    {
        b.Status = BdeBookingStatus.Finished;
        b.EndedAt = DateTime.Now;
        SetAuditModified(b);

        if (b.BookingType == BdeBookingType.Production && (goodQty.HasValue || scrapQty.HasValue))
        {
            var q = CreatePartialQuantity(b, goodQty ?? 0, scrapQty ?? 0);
            q.IsFinal = true;
            _ctx.BdeBookingQuantities.Add(q);
        }
    }

    private BdeBookingQuantity CreatePartialQuantity(BdeBooking b, decimal good, decimal scrap)
    {
        var q = new BdeBookingQuantity
        {
            BdeBookingId = b.Id,
            BdeOperatorId = b.BdeOperatorId,
            GoodQuantity = good,
            ScrapQuantity = scrap,
            IsFinal = false,
            ReportedAt = DateTime.Now
        };
        SetAudit(q);
        return q;
    }

    private void SetAudit(AuditableEntity e)
    {
        e.CreatedAt = DateTime.Now;
        e.CreatedBy = _userSvc.GetDisplayName();
        e.CreatedByWindows = _userSvc.GetWindowsUserName();
    }

    private void SetAuditModified(AuditableEntity e)
    {
        e.ModifiedAt = DateTime.Now;
        e.ModifiedBy = _userSvc.GetDisplayName();
        e.ModifiedByWindows = _userSvc.GetWindowsUserName();
    }
}
```

- [ ] **Step 5: Fake-User-Service und Test-Daten-Helper**

Datei `IdealAkeWms.Tests/Helpers/FakeCurrentUserService.cs` (falls nicht schon vorhanden aus anderen Tests):

```csharp
using IdealAkeWms.Services;

public class FakeCurrentUserService : ICurrentUserService
{
    public string GetWindowsUserName() => "TEST\\user";
    public string GetDisplayName() => "Test User";
    public int? GetCurrentAppUserId() => 1;
    public string? GetCurrentAppUserName() => "testuser";
    public bool IsLoggedIn() => true;
    public Task<bool> HasRoleAsync(string roleKey) => Task.FromResult(true);
    public Task<bool> HasAnyRoleAsync(params string[] roleKeys) => Task.FromResult(true);
    public Task<bool> IsAdminAsync() => Task.FromResult(true);
    public Task<bool> HasMasterDataAccessAsync() => Task.FromResult(true);
    public Task<bool> CanPickAsync() => Task.FromResult(true);
    public Task<bool> CanViewTrackingAsync() => Task.FromResult(true);
    public Task<bool> CanReportOperationsAsync() => Task.FromResult(true);
    public Task<bool> CanAccessStockAsync() => Task.FromResult(true);
    public Task<bool> CanTransferStockAsync() => Task.FromResult(true);
    public Task<bool> CanManagePickingReleaseAsync() => Task.FromResult(true);
    public Task<bool> CanUseBdeAsync() => Task.FromResult(true);
    public Task<bool> CanManageBdeShiftleadAsync() => Task.FromResult(true);
    public Task<bool> CanManageBdeAdminAsync() => Task.FromResult(true);
}
```

Datei `IdealAkeWms.Tests/Helpers/BdeTestData.cs`:

```csharp
using IdealAkeWms.Data;
using IdealAkeWms.Models;

namespace IdealAkeWms.Tests.Helpers;

public static class BdeTestData
{
    public static void Seed(ApplicationDbContext ctx, out int operatorId, out int workOperationId, out int workplaceId, out int terminalId)
    {
        var user = new User { UserName = "termuser", DisplayName = "Terminal", PasswordHash = "x", CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t" };
        var op = new BdeOperator { PersonnelNumber = "P1", FirstName = "Max", LastName = "Muster", IsActive = true, CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t" };
        var wp = new ProductionWorkplace { Name = "Werkbank 1", IsActive = true, CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t" };
        var po = new ProductionOrder { OrderNumber = "FA-100", Quantity = 10, CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t" };
        ctx.Users.Add(user); ctx.BdeOperators.Add(op); ctx.ProductionWorkplaces.Add(wp); ctx.ProductionOrders.Add(po);
        ctx.SaveChanges();

        var wo = new WorkOperation { ProductionOrderId = po.Id, OperationNumber = "10", Name = "Fräsen", Sequence = 1, CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t" };
        ctx.WorkOperations.Add(wo);
        var term = new BdeTerminal { UserId = user.Id, DefaultProductionWorkplaceId = wp.Id, CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t" };
        ctx.BdeTerminals.Add(term);
        ctx.SaveChanges();

        operatorId = op.Id;
        workOperationId = wo.Id;
        workplaceId = wp.Id;
        terminalId = term.Id;
    }

    public static int SeedSecondWorkOperation(ApplicationDbContext ctx, int productionOrderId)
    {
        var wo = new WorkOperation { ProductionOrderId = productionOrderId, OperationNumber = "20", Name = "Bohren", Sequence = 2, CreatedAt = DateTime.Now, CreatedBy = "t", CreatedByWindows = "t" };
        ctx.WorkOperations.Add(wo);
        ctx.SaveChanges();
        return wo.Id;
    }
}
```

- [ ] **Step 6: Tests ausführen, alle grün**

```
dotnet test IdealAkeWms.Tests --filter FullyQualifiedName~BdeBookingServiceTests
```

Expected: 18+ passed.

- [ ] **Step 7: DI registrieren + Commit**

`Program.cs`:

```csharp
builder.Services.AddScoped<IBdeBookingService, BdeBookingService>();
builder.Services.AddScoped<IBdeBookingRepository, BdeBookingRepository>();
builder.Services.AddScoped<IBdeTerminalRepository, BdeTerminalRepository>();
builder.Services.AddScoped<IBdeActivityRepository, BdeActivityRepository>();
```

Commit: `feat(bde): add BdeBookingService with state machine (TDD)`.

---

## Task 15: Rollen-Seeding in Program.cs

**Files:**
- Modify: `IdealAkeWms/Program.cs`

- [ ] **Step 1: Rollen-Seed-Block erweitern**

Im bestehenden Rollen-Seeding-Block (analog zum `Leitstand`-Eintrag in Commit `38_`) folgende Einträge ergänzen:

```csharp
if (!await roleRepo.ExistsAsync(RoleKeys.BdeUser))
    await roleRepo.AddAsync(new Role { Key = RoleKeys.BdeUser, DisplayName = "BDE-Mitarbeiter", CreatedAt = DateTime.Now, CreatedBy = "System", CreatedByWindows = "System" });
if (!await roleRepo.ExistsAsync(RoleKeys.BdeShiftlead))
    await roleRepo.AddAsync(new Role { Key = RoleKeys.BdeShiftlead, DisplayName = "BDE-Schichtleiter", CreatedAt = DateTime.Now, CreatedBy = "System", CreatedByWindows = "System" });
if (!await roleRepo.ExistsAsync(RoleKeys.BdeAdmin))
    await roleRepo.AddAsync(new Role { Key = RoleKeys.BdeAdmin, DisplayName = "BDE-Admin", CreatedAt = DateTime.Now, CreatedBy = "System", CreatedByWindows = "System" });
```

(Die exakten API-Calls aus dem existierenden Code übernehmen — Pattern siehe bisheriges Leitstand-Seeding in Program.cs.)

- [ ] **Step 2: App starten, DB verifizieren**

Lokale DB prüfen: `SELECT * FROM Roles WHERE [Key] LIKE 'bde%'` → 3 Zeilen.

- [ ] **Step 3: Commit**

```bash
git add IdealAkeWms/Program.cs
git commit -m "feat(bde): seed BDE roles on startup"
```

---

## Task 16: BdeMasterDataController + Operatoren-CRUD

**Files:**
- Create: `IdealAkeWms/Controllers/BdeMasterDataController.cs`
- Create: `IdealAkeWms/Models/ViewModels/BdeOperatorEditViewModel.cs`
- Create: `IdealAkeWms/Views/BdeMasterData/Index.cshtml`
- Create: `IdealAkeWms/Views/BdeMasterData/EditOperator.cshtml`

Referenz-Pattern: `UsersController` + `Views/Users/`. Tabs-Pattern: `Views/Shared/_TabNav.cshtml` oder Bootstrap-Nav-Tabs inline.

- [ ] **Step 1: ViewModel**

`BdeOperatorEditViewModel`:

```csharp
using System.ComponentModel.DataAnnotations;

namespace IdealAkeWms.Models.ViewModels;

public class BdeOperatorEditViewModel
{
    public int Id { get; set; }

    [Required(ErrorMessage = "Personalnummer ist erforderlich")]
    [StringLength(50)]
    [Display(Name = "Personalnummer")]
    public string PersonnelNumber { get; set; } = string.Empty;

    [Required(ErrorMessage = "Vorname ist erforderlich")]
    [StringLength(100)]
    [Display(Name = "Vorname")]
    public string FirstName { get; set; } = string.Empty;

    [Required(ErrorMessage = "Nachname ist erforderlich")]
    [StringLength(100)]
    [Display(Name = "Nachname")]
    public string LastName { get; set; } = string.Empty;

    [Display(Name = "Aktiv")]
    public bool IsActive { get; set; } = true;

    [Display(Name = "WMS-Benutzer")]
    public int? UserId { get; set; }
}
```

- [ ] **Step 2: Controller-Skelett**

```csharp
using IdealAkeWms.Data.Repositories;
using IdealAkeWms.Filters;
using IdealAkeWms.Models;
using IdealAkeWms.Models.ViewModels;
using IdealAkeWms.Services;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.Rendering;

namespace IdealAkeWms.Controllers;

[RequireBdeShiftleadAccess]
public class BdeMasterDataController : Controller
{
    private readonly IBdeOperatorRepository _ops;
    private readonly IBdeActivityRepository _activities;
    private readonly IBdeTerminalRepository _terminals;
    private readonly IUserRepository _users;
    private readonly ICurrentUserService _userSvc;

    public BdeMasterDataController(IBdeOperatorRepository ops, IBdeActivityRepository activities,
        IBdeTerminalRepository terminals, IUserRepository users, ICurrentUserService userSvc)
    {
        _ops = ops; _activities = activities; _terminals = terminals; _users = users; _userSvc = userSvc;
    }

    public async Task<IActionResult> Index(string tab = "operators")
    {
        ViewBag.Tab = tab;
        ViewBag.Operators = await _ops.GetAllAsync();
        ViewBag.Activities = await _activities.GetAllAsync();
        ViewBag.Terminals = await _terminals.GetAllAsync();
        return View();
    }

    [HttpGet]
    public async Task<IActionResult> EditOperator(int? id)
    {
        if (id == null) return View(new BdeOperatorEditViewModel());
        var op = await _ops.GetByIdAsync(id.Value);
        if (op == null) return NotFound();
        return View(new BdeOperatorEditViewModel {
            Id = op.Id, PersonnelNumber = op.PersonnelNumber, FirstName = op.FirstName,
            LastName = op.LastName, IsActive = op.IsActive, UserId = op.UserId
        });
    }

    [HttpPost]
    [ValidateAntiForgeryToken]
    public async Task<IActionResult> EditOperator(BdeOperatorEditViewModel vm)
    {
        if (!ModelState.IsValid) return View(vm);

        if (vm.Id == 0)
        {
            var op = new BdeOperator {
                PersonnelNumber = vm.PersonnelNumber, FirstName = vm.FirstName, LastName = vm.LastName,
                IsActive = vm.IsActive, UserId = vm.UserId,
                CreatedAt = DateTime.Now,
                CreatedBy = _userSvc.GetDisplayName(),
                CreatedByWindows = _userSvc.GetWindowsUserName()
            };
            await _ops.AddAsync(op);
            TempData["SuccessMessage"] = $"BDE-Anwender '{op.DisplayName}' angelegt.";
        }
        else
        {
            var op = await _ops.GetByIdAsync(vm.Id);
            if (op == null) return NotFound();
            op.PersonnelNumber = vm.PersonnelNumber;
            op.FirstName = vm.FirstName;
            op.LastName = vm.LastName;
            op.IsActive = vm.IsActive;
            op.UserId = vm.UserId;
            op.ModifiedAt = DateTime.Now;
            op.ModifiedBy = _userSvc.GetDisplayName();
            op.ModifiedByWindows = _userSvc.GetWindowsUserName();
            await _ops.UpdateAsync(op);
            TempData["SuccessMessage"] = $"BDE-Anwender '{op.DisplayName}' aktualisiert.";
        }
        return RedirectToAction(nameof(Index), new { tab = "operators" });
    }
}
```

(Actions `EditActivity` und `EditTerminal` werden in Tasks 17/18 ergänzt — jetzt erst mal nur Operatoren.)

- [ ] **Step 3: Views**

`Views/BdeMasterData/Index.cshtml`: Bootstrap-Tab-Navigation mit 3 Panes (Operators, Activities, Terminals). Pane "Operators" zeigt Tabelle mit Filtern (analog `Users/Index`), "Neu"-Button führt zu `EditOperator`. Andere Panes für jetzt leerer Placeholder.

`Views/BdeMasterData/EditOperator.cshtml`: Standard-Form mit `asp-for` für alle Felder, User-Dropdown aus `ViewBag.Users` (ICurrentUserService wählt nur Users anzeigen, die noch keinen BdeOperator haben — das regeln wir in Controller-Action).

- [ ] **Step 4: Route-Defaults + Commit**

Keine Route-Änderung nötig (Standard-MVC-Pattern). Testen:

```
dotnet build && dotnet run --project IdealAkeWms
```

Browser: `/BdeMasterData` → zeigt Tabs. Neuer Operator anlegen → Liste zeigt ihn.

Commit: `feat(bde): add BdeMasterData controller with operators CRUD`.

---

## Task 17: Aktivitäten-CRUD in BdeMasterDataController

**Files:**
- Modify: `IdealAkeWms/Controllers/BdeMasterDataController.cs`
- Create: `IdealAkeWms/Models/ViewModels/BdeActivityEditViewModel.cs`
- Create: `IdealAkeWms/Views/BdeMasterData/EditActivity.cshtml`
- Modify: `IdealAkeWms/Views/BdeMasterData/Index.cshtml`

Analog Task 16: ViewModel mit Code/Name/IsActive, `EditActivity`-Actions (GET+POST), View mit Form. Index-Tab "Activities" befüllen (Tabelle + Neu-Button).

Acceptance-Test manuell: neue Activity "WART - Wartung" anlegen → erscheint im Tab.

Commit: `feat(bde): add activity CRUD to BdeMasterData`.

---

## Task 18: Terminal-CRUD in BdeMasterDataController

**Files:**
- Modify: `IdealAkeWms/Controllers/BdeMasterDataController.cs`
- Create: `IdealAkeWms/Models/ViewModels/BdeTerminalEditViewModel.cshtml`
- Create: `IdealAkeWms/Views/BdeMasterData/EditTerminal.cshtml`

Besonderheiten:
- `EditTerminal` erfordert `[RequireBdeAdminAccess]` an der Action (Methoden-Filter überschreibt Controller-Filter).
- User-Dropdown: alle Users (inkl. sich selbst), `ProductionWorkplace`-Dropdown: aktive Werkbänke.
- Validierung im POST: ein User kann nur ein Terminal haben (Unique-Index fängt das DB-seitig ab, aber Client-seitige Fehlermeldung freundlich).

Commit: `feat(bde): add terminal CRUD to BdeMasterData`.

---

## Task 19: BdeApiController — Scan-Lookup-Endpoints

**Files:**
- Create: `IdealAkeWms/Controllers/BdeApiController.cs`
- Create: `IdealAkeWms.Tests/Controllers/BdeApiControllerTests.cs`

API-Endpoints für Terminal-JavaScript:
- `GET /api/bde/operator/{personnelNumber}` → Operator-Info oder 404
- `GET /api/bde/workoperation?faNumber={fa}&opNumber={op}` → WorkOperation-Info oder 404
- `GET /api/bde/activities` → Liste aktive Aktivitäten
- `GET /api/bde/operator/{id}/active-booking` → aktuelle offene Buchung des Operators (oder null)
- `GET /api/bde/workoperation/{id}/latest-paused` → jüngste pausierte Buchung

- [ ] **Step 1: Controller schreiben**

```csharp
using IdealAkeWms.Data.Repositories;
using IdealAkeWms.Filters;
using Microsoft.AspNetCore.Mvc;

namespace IdealAkeWms.Controllers;

[ApiController]
[Route("api/bde")]
[RequireBdeUserAccess]
public class BdeApiController : ControllerBase
{
    private readonly IBdeOperatorRepository _ops;
    private readonly IBdeActivityRepository _activities;
    private readonly IBdeBookingRepository _bookings;
    private readonly IWorkOperationRepository _workOps;

    public BdeApiController(IBdeOperatorRepository ops, IBdeActivityRepository activities,
        IBdeBookingRepository bookings, IWorkOperationRepository workOps)
    {
        _ops = ops; _activities = activities; _bookings = bookings; _workOps = workOps;
    }

    [HttpGet("operator/{personnelNumber}")]
    public async Task<IActionResult> GetOperator(string personnelNumber)
    {
        var op = await _ops.GetByPersonnelNumberAsync(personnelNumber);
        if (op == null) return NotFound();
        return Ok(new { id = op.Id, displayName = op.DisplayName, personnelNumber = op.PersonnelNumber });
    }

    [HttpGet("workoperation")]
    public async Task<IActionResult> GetWorkOperation([FromQuery] string faNumber, [FromQuery] string opNumber)
    {
        // FA-Nummer kann Komma-Suffix haben → vorher splitten
        faNumber = faNumber.Split(',')[0].Trim();
        var wo = await _workOps.GetByFaAndOperationAsync(faNumber, opNumber);
        if (wo == null) return NotFound();
        return Ok(new {
            id = wo.Id,
            operationNumber = wo.OperationNumber,
            name = wo.Name,
            orderNumber = wo.ProductionOrder.OrderNumber,
            articleNumber = wo.ProductionOrder.ArticleNumber,
            description = wo.ProductionOrder.Description1,
            workplaceId = wo.ProductionWorkplaceId,
            workplaceName = wo.ProductionWorkplace?.Name
        });
    }

    [HttpGet("activities")]
    public async Task<IActionResult> GetActivities() =>
        Ok((await _activities.GetAllActiveAsync()).Select(a => new { id = a.Id, code = a.Code, name = a.Name }));

    [HttpGet("operator/{id:int}/active-booking")]
    public async Task<IActionResult> GetActiveBooking(int id)
    {
        var b = await _bookings.GetActiveForOperatorAsync(id);
        if (b == null) return Ok(new { booking = (object?)null });
        return Ok(new {
            booking = new {
                id = b.Id,
                bookingType = b.BookingType.ToString(),
                status = b.Status.ToString(),
                startedAt = b.StartedAt,
                workOperationId = b.WorkOperationId,
                bdeActivityId = b.BdeActivityId
            }
        });
    }

    [HttpGet("workoperation/{id:int}/latest-paused")]
    public async Task<IActionResult> GetLatestPaused(int id)
    {
        var b = await _bookings.GetLatestPausedForWorkOperationAsync(id);
        if (b == null) return NotFound();
        return Ok(new { id = b.Id, bookingType = b.BookingType.ToString(), operatorName = b.BdeOperator.DisplayName });
    }
}
```

- [ ] **Step 2: `IWorkOperationRepository.GetByFaAndOperationAsync`**

Falls noch nicht vorhanden, in `IWorkOperationRepository`/`WorkOperationRepository` ergänzen:

```csharp
public Task<WorkOperation?> GetByFaAndOperationAsync(string faNumber, string operationNumber) =>
    _ctx.WorkOperations
        .Include(w => w.ProductionOrder)
        .Include(w => w.ProductionWorkplace)
        .FirstOrDefaultAsync(w => w.ProductionOrder.OrderNumber == faNumber && w.OperationNumber == operationNumber);
```

Plus Test.

- [ ] **Step 3: Tests für BdeApiController**

Controller-Tests mit Moq und `Mock<IBdeOperatorRepository>` etc. Teste Happy-Path und 404-Fälle pro Endpoint.

- [ ] **Step 4: Build + Test + Commit**

```bash
dotnet test IdealAkeWms.Tests
git add ...
git commit -m "feat(bde): add API controller for scan lookups"
```

---

## Task 20: BdeTerminalController + Scan-UI

**Files:**
- Create: `IdealAkeWms/Controllers/BdeTerminalController.cs`
- Create: `IdealAkeWms/Views/BdeTerminal/Index.cshtml`
- Create: `IdealAkeWms/wwwroot/js/bde-terminal.js`
- Create: `IdealAkeWms/wwwroot/css/bde.css`

- [ ] **Step 1: Controller**

```csharp
using IdealAkeWms.Data.Repositories;
using IdealAkeWms.Filters;
using IdealAkeWms.Models;
using IdealAkeWms.Services;
using Microsoft.AspNetCore.Mvc;

namespace IdealAkeWms.Controllers;

[RequireBdeUserAccess]
public class BdeTerminalController : Controller
{
    private readonly IBdeTerminalRepository _terminals;
    private readonly IBdeBookingService _bookingSvc;
    private readonly ICurrentUserService _userSvc;
    private readonly IProductionWorkplaceRepository _workplaces;

    public BdeTerminalController(IBdeTerminalRepository terminals, IBdeBookingService bookingSvc,
        ICurrentUserService userSvc, IProductionWorkplaceRepository workplaces)
    {
        _terminals = terminals; _bookingSvc = bookingSvc; _userSvc = userSvc; _workplaces = workplaces;
    }

    public async Task<IActionResult> Index(int? workplaceId)
    {
        var appUserId = _userSvc.GetCurrentAppUserId();
        if (appUserId == null) return RedirectToAction("Login", "Account");

        var terminal = await _terminals.GetByUserIdAsync(appUserId.Value);
        if (terminal == null)
        {
            TempData["WarningMessage"] = "Für diesen Benutzer ist kein Terminal konfiguriert. Bitte an den BDE-Admin wenden.";
            return RedirectToAction("Index", "Home");
        }

        var activeWorkplaceId = workplaceId ?? terminal.DefaultProductionWorkplaceId;
        ViewBag.TerminalId = terminal.Id;
        ViewBag.WorkplaceId = activeWorkplaceId;
        ViewBag.WorkplaceName = (await _workplaces.GetByIdAsync(activeWorkplaceId))?.Name;
        ViewBag.AllWorkplaces = await _workplaces.GetAllActiveAsync();
        ViewBag.DefaultWorkplaceId = terminal.DefaultProductionWorkplaceId;
        return View();
    }

    // POST-Endpoints für die Aktions-Buttons:
    [HttpPost, ValidateAntiForgeryToken]
    public async Task<IActionResult> StartSetup(int operatorId, int workOperationId, int workplaceId, int terminalId)
    {
        var result = await _bookingSvc.StartSetupAsync(operatorId, workOperationId, workplaceId, terminalId);
        return Json(MapResult(result));
    }

    [HttpPost, ValidateAntiForgeryToken]
    public async Task<IActionResult> StartProduction(int operatorId, int workOperationId, int workplaceId, int terminalId)
    {
        var result = await _bookingSvc.StartProductionAsync(operatorId, workOperationId, workplaceId, terminalId);
        return Json(MapResult(result));
    }

    [HttpPost, ValidateAntiForgeryToken]
    public async Task<IActionResult> StartActivity(int operatorId, int activityId, int workplaceId, int terminalId)
    {
        var result = await _bookingSvc.StartActivityAsync(operatorId, activityId, workplaceId, terminalId);
        return Json(MapResult(result));
    }

    [HttpPost, ValidateAntiForgeryToken]
    public async Task<IActionResult> Pause(int bookingId, decimal? goodQty, decimal? scrapQty)
    {
        var result = await _bookingSvc.PauseAsync(bookingId, goodQty, scrapQty);
        return Json(MapResult(result));
    }

    [HttpPost, ValidateAntiForgeryToken]
    public async Task<IActionResult> Resume(int pausedBookingId, int operatorId, BdeBookingType resumeAs, int workplaceId, int terminalId)
    {
        var result = await _bookingSvc.ResumeAsync(pausedBookingId, operatorId, resumeAs, workplaceId, terminalId);
        return Json(MapResult(result));
    }

    [HttpPost, ValidateAntiForgeryToken]
    public async Task<IActionResult> Finish(int bookingId, decimal? goodQty, decimal? scrapQty)
    {
        var result = await _bookingSvc.FinishAsync(bookingId, goodQty, scrapQty);
        return Json(MapResult(result));
    }

    [HttpPost, ValidateAntiForgeryToken]
    public async Task<IActionResult> ReportPartial(int bookingId, decimal goodQty, decimal scrapQty)
    {
        var result = await _bookingSvc.ReportPartialQuantityAsync(bookingId, goodQty, scrapQty);
        return Json(MapResult(result));
    }

    private static object MapResult(BdeBookingResult r) => new
    {
        outcome = r.Outcome.ToString(),
        bookingId = r.Booking?.Id,
        collidingOperator = r.CollidingBooking?.BdeOperator?.DisplayName,
        collidingWorkplace = r.CollidingBooking?.ProductionWorkplace?.Name,
        collidingSince = r.CollidingBooking?.StartedAt,
        message = r.Message
    };
}
```

- [ ] **Step 2: View `Views/BdeTerminal/Index.cshtml`**

```html
@{
    ViewData["Title"] = "BDE-Terminal";
}

<div class="container-fluid bde-terminal">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
        <h2>
            BDE-Terminal
            <small class="text-muted">Werkbank:
                <select id="workplaceSwitch" class="form-select form-select-sm d-inline-block w-auto">
                    @foreach (var wp in (IEnumerable<dynamic>)ViewBag.AllWorkplaces)
                    {
                        <option value="@wp.Id" selected="@(wp.Id == ViewBag.WorkplaceId)">@wp.Name</option>
                    }
                </select>
            </small>
        </h2>
        <div class="text-muted small">
            Angemeldet: @(User.Identity?.Name) | Session läuft bis: <span id="sessionExpiry">—</span>
        </div>
    </div>

    <div class="row g-3">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">Scannen</div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label">Personalnummer oder FA-AG scannen</label>
                        <input type="text" id="scanInput" class="form-control form-control-lg" autofocus placeholder="Scannen…" />
                    </div>
                    <div id="scanFeedback" class="small text-muted"></div>
                    <button id="btnActivity" class="btn btn-outline-secondary mt-2">Ungeplante Tätigkeit</button>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">Aktuelle Buchung</div>
                <div class="card-body" id="currentBooking">
                    <em class="text-muted">Keine aktive Buchung</em>
                </div>
            </div>
        </div>
    </div>

    <!-- Aktions-Buttons erscheinen dynamisch via JS -->
    <div id="actionPanel" class="mt-3"></div>

    <!-- Mengen-Modal -->
    <div class="modal fade" id="quantityModal" tabindex="-1">
      <div class="modal-dialog"><div class="modal-content">
        <div class="modal-header"><h5 class="modal-title">Mengen melden</h5></div>
        <div class="modal-body">
          <div class="mb-3"><label>Gutmenge</label><input type="number" id="inputGood" class="form-control form-control-lg" step="1" min="0" /></div>
          <div class="mb-3"><label>Ausschuss</label><input type="number" id="inputScrap" class="form-control form-control-lg" step="1" min="0" value="0" /></div>
        </div>
        <div class="modal-footer"><button id="btnQtyCancel" class="btn btn-secondary">Abbrechen</button><button id="btnQtyOk" class="btn btn-primary">Bestätigen</button></div>
      </div></div>
    </div>

    <!-- Kollisions-Modal -->
    <div class="modal fade" id="collisionModal" tabindex="-1">
      <div class="modal-dialog"><div class="modal-content">
        <div class="modal-header bg-warning"><h5 class="modal-title">Arbeitsgang bereits in Arbeit</h5></div>
        <div class="modal-body"><p id="collisionText"></p></div>
        <div class="modal-footer"><button class="btn btn-primary" data-bs-dismiss="modal">OK</button></div>
      </div></div>
    </div>

    <!-- Aktivitäts-Modal -->
    <div class="modal fade" id="activityModal" tabindex="-1">
      <div class="modal-dialog"><div class="modal-content">
        <div class="modal-header"><h5 class="modal-title">Ungeplante Tätigkeit</h5></div>
        <div class="modal-body">
            <label>Kategorie</label>
            <select id="activitySelect" class="form-select form-select-lg"></select>
        </div>
        <div class="modal-footer"><button class="btn btn-secondary" data-bs-dismiss="modal">Abbrechen</button><button id="btnActivityOk" class="btn btn-primary">Starten</button></div>
      </div></div>
    </div>
</div>

@section Scripts {
    <input type="hidden" id="terminalId" value="@ViewBag.TerminalId" />
    <input type="hidden" id="workplaceId" value="@ViewBag.WorkplaceId" />
    @Html.AntiForgeryToken()
    <script src="~/js/bde-terminal.js" asp-append-version="true"></script>
}
```

- [ ] **Step 3: JavaScript `bde-terminal.js`**

Datei mit folgenden Kern-Funktionen (ausführliche Implementation im Plan — nicht TBD):

```javascript
(function () {
    'use strict';

    const terminalId = parseInt(document.getElementById('terminalId').value);
    let workplaceId = parseInt(document.getElementById('workplaceId').value);
    const token = document.querySelector('input[name="__RequestVerificationToken"]').value;

    let currentOperator = null;   // {id, displayName}
    let currentWorkOp = null;     // {id, orderNumber, operationNumber, name}

    const scanInput = document.getElementById('scanInput');
    const scanFeedback = document.getElementById('scanFeedback');
    const currentBooking = document.getElementById('currentBooking');
    const actionPanel = document.getElementById('actionPanel');

    // Scan-Handler: Enter-Taste = Scan abgeschlossen
    scanInput.addEventListener('keydown', async (e) => {
        if (e.key !== 'Enter') return;
        e.preventDefault();
        const raw = scanInput.value.trim();
        scanInput.value = '';
        if (!raw) return;
        await handleScan(raw);
    });

    async function handleScan(raw) {
        // Heuristik: enthält Komma oder Bindestrich + nur Ziffern-Prefix → FA-AG
        // Alles andere → Personalnummer
        // (Für Produktion ggf. mit Prefix-Konvention aus QR-Code arbeiten)
        if (/^[A-Z0-9\-]+,[0-9]+/i.test(raw) || raw.includes('/')) {
            await scanFaAg(raw);
        } else {
            await scanOperator(raw);
        }
        renderState();
    }

    async function scanOperator(personnelNumber) {
        const r = await fetch(`/api/bde/operator/${encodeURIComponent(personnelNumber)}`);
        if (!r.ok) { scanFeedback.textContent = 'Unbekannte Personalnummer'; return; }
        currentOperator = await r.json();
        scanFeedback.textContent = `Operator: ${currentOperator.displayName}`;
    }

    async function scanFaAg(raw) {
        // Format erwartet: "FA-123456,10" oder "FA-123456/10"
        const [fa, op] = raw.split(/[,\/]/);
        const r = await fetch(`/api/bde/workoperation?faNumber=${encodeURIComponent(fa)}&opNumber=${encodeURIComponent(op)}`);
        if (!r.ok) { scanFeedback.textContent = 'Arbeitsgang nicht gefunden'; return; }
        currentWorkOp = await r.json();
        scanFeedback.textContent = `AG: ${currentWorkOp.orderNumber} / ${currentWorkOp.operationNumber} — ${currentWorkOp.name}`;
    }

    async function renderState() {
        if (!currentOperator) { actionPanel.innerHTML = '<em class="text-muted">Zuerst Operator scannen</em>'; return; }
        // Laufende Buchung holen
        const r = await fetch(`/api/bde/operator/${currentOperator.id}/active-booking`);
        const data = await r.json();
        const active = data.booking;
        if (active) {
            currentBooking.innerHTML = renderActive(active);
            actionPanel.innerHTML = renderActionsForActive(active);
        } else {
            currentBooking.innerHTML = '<em class="text-muted">Keine aktive Buchung</em>';
            actionPanel.innerHTML = currentWorkOp ? renderStartButtons() : '';
        }
        bindActionHandlers();
    }

    function renderActive(b) { /* HTML für aktive Buchung */ return `<strong>${b.bookingType}</strong> seit ${new Date(b.startedAt).toLocaleTimeString()}`; }
    function renderActionsForActive(b) {
        if (b.status === 'Running' && b.bookingType === 'Setup') return buttons(['pause','finishSetup','startProduction']);
        if (b.status === 'Running' && b.bookingType === 'Production') return buttons(['pause','reportPartial','finish']);
        if (b.status === 'Running' && b.bookingType === 'Activity') return buttons(['finishActivity']);
        return '';
    }
    function renderStartButtons() { return buttons(['startSetup','startProduction']); }
    function buttons(ids) {
        const labels = { startSetup:'Rüsten starten', startProduction:'Produktion starten', pause:'Pause', finish:'Beenden (mit Mengen)', finishSetup:'Rüsten beenden', finishActivity:'Beenden', reportPartial:'Teilfertigmeldung' };
        return ids.map(id => `<button data-action="${id}" class="btn btn-primary btn-lg m-1">${labels[id]}</button>`).join('');
    }

    function bindActionHandlers() {
        actionPanel.querySelectorAll('button[data-action]').forEach(btn => {
            btn.addEventListener('click', () => handleAction(btn.dataset.action));
        });
    }

    async function handleAction(action) {
        switch (action) {
            case 'startSetup': await post('/BdeTerminal/StartSetup', { operatorId: currentOperator.id, workOperationId: currentWorkOp.id, workplaceId, terminalId }); break;
            case 'startProduction': await post('/BdeTerminal/StartProduction', { operatorId: currentOperator.id, workOperationId: currentWorkOp.id, workplaceId, terminalId }); break;
            case 'pause': await promptQty(async (good, scrap) => await post('/BdeTerminal/Pause', { bookingId: await activeId(), goodQty: good, scrapQty: scrap })); break;
            case 'finish': await promptQty(async (good, scrap) => await post('/BdeTerminal/Finish', { bookingId: await activeId(), goodQty: good, scrapQty: scrap })); break;
            case 'finishSetup': await post('/BdeTerminal/Finish', { bookingId: await activeId() }); break;
            case 'finishActivity': await post('/BdeTerminal/Finish', { bookingId: await activeId() }); break;
            case 'reportPartial': await promptQty(async (good, scrap) => await post('/BdeTerminal/ReportPartial', { bookingId: await activeId(), goodQty: good, scrapQty: scrap })); break;
        }
        await renderState();
    }

    async function activeId() {
        const r = await fetch(`/api/bde/operator/${currentOperator.id}/active-booking`);
        return (await r.json()).booking?.id;
    }

    async function post(url, data) {
        const form = new FormData();
        Object.keys(data).forEach(k => { if (data[k] !== undefined && data[k] !== null) form.append(k, data[k]); });
        form.append('__RequestVerificationToken', token);
        const r = await fetch(url, { method: 'POST', body: form });
        const json = await r.json();
        if (json.outcome === 'CollisionOtherOperator') {
            document.getElementById('collisionText').textContent =
                `AG ist bereits in Arbeit durch ${json.collidingOperator} an ${json.collidingWorkplace} seit ${new Date(json.collidingSince).toLocaleTimeString()}.`;
            bootstrap.Modal.getOrCreateInstance(document.getElementById('collisionModal')).show();
        } else if (json.outcome === 'QuantityRequired') {
            scanFeedback.textContent = 'Mengen-Eingabe erforderlich — bitte aktuelle Buchung beenden.';
        }
    }

    function promptQty(callback) {
        return new Promise(resolve => {
            const modal = bootstrap.Modal.getOrCreateInstance(document.getElementById('quantityModal'));
            modal.show();
            document.getElementById('inputGood').value = '';
            document.getElementById('inputScrap').value = '0';
            document.getElementById('btnQtyOk').onclick = async () => {
                const g = parseFloat(document.getElementById('inputGood').value || '0');
                const s = parseFloat(document.getElementById('inputScrap').value || '0');
                modal.hide();
                await callback(g, s);
                resolve();
            };
            document.getElementById('btnQtyCancel').onclick = () => { modal.hide(); resolve(); };
        });
    }

    // Aktivitäts-Flow
    document.getElementById('btnActivity').addEventListener('click', async () => {
        if (!currentOperator) { scanFeedback.textContent = 'Zuerst Operator scannen'; return; }
        const r = await fetch('/api/bde/activities');
        const list = await r.json();
        const sel = document.getElementById('activitySelect');
        sel.innerHTML = list.map(a => `<option value="${a.id}">${a.code} — ${a.name}</option>`).join('');
        bootstrap.Modal.getOrCreateInstance(document.getElementById('activityModal')).show();
    });
    document.getElementById('btnActivityOk').addEventListener('click', async () => {
        const activityId = parseInt(document.getElementById('activitySelect').value);
        bootstrap.Modal.getOrCreateInstance(document.getElementById('activityModal')).hide();
        await post('/BdeTerminal/StartActivity', { operatorId: currentOperator.id, activityId, workplaceId, terminalId });
        await renderState();
    });

    // Werkbank-Umschaltung
    document.getElementById('workplaceSwitch').addEventListener('change', (e) => {
        window.location.href = '/BdeTerminal/Index?workplaceId=' + e.target.value;
    });

    renderState();
})();
```

- [ ] **Step 4: CSS `bde.css`**

Touch-freundliche Buttons (`min-height: 60px`, größere Schrift), Corporate-Farben:

```css
.bde-terminal .btn-lg { min-height: 60px; font-size: 1.2rem; }
.bde-terminal .form-control-lg { font-size: 1.3rem; }
.bde-terminal input[type="number"] { font-size: 1.5rem; text-align: center; }
```

In `_Layout.cshtml` einbinden (oder nur auf BDE-Seiten via Section).

- [ ] **Step 5: Manuelle QA**

- Lokal starten, als Admin einloggen.
- Terminal anlegen (`/BdeMasterData` Terminals-Tab) → User zuweisen, Werkbank wählen.
- Einen BDE-Operator anlegen.
- Einen Produktionsauftrag mit WorkOperation benötigen (über Sage-Import oder direkt in DB einfügen).
- `/BdeTerminal` öffnen → Personalnummer eingeben + Enter → Feedback "Operator: …"
- FA-AG eingeben (z.B. "FA-100,10") → "Rüsten starten" / "Produktion starten"
- Pause, Beenden, Mengen-Dialog testen.
- Zweites Browserfenster als anderer Operator → selber AG → Kollisions-Modal.

- [ ] **Step 6: Commit**

```bash
git add IdealAkeWms/Controllers/BdeTerminalController.cs IdealAkeWms/Views/BdeTerminal/ IdealAkeWms/wwwroot/js/bde-terminal.js IdealAkeWms/wwwroot/css/bde.css
git commit -m "feat(bde): add terminal UI with scan-based booking"
```

---

## Task 21: BdeCockpitController + Polling-View

**Files:**
- Create: `IdealAkeWms/Controllers/BdeCockpitController.cs`
- Create: `IdealAkeWms/Views/BdeCockpit/Index.cshtml`
- Create: `IdealAkeWms/wwwroot/js/bde-cockpit.js`
- Modify: `IdealAkeWms/Controllers/BdeApiController.cs` (+ Cockpit-Endpoint)

- [ ] **Step 1: Cockpit-API-Endpoint**

In `BdeApiController` ergänzen, aber mit `[RequireBdeShiftleadAccess]` auf der Action (überschreibt Controller-Filter):

```csharp
[HttpGet("cockpit")]
[RequireBdeShiftleadAccess]
public async Task<IActionResult> GetCockpit()
{
    var workplaces = await _workplaces.GetAllActiveAsync();
    var activeBookings = await _bookings.GetActiveCockpitAsync();

    var tiles = workplaces.Select(wp =>
    {
        var b = activeBookings.FirstOrDefault(x => x.ProductionWorkplaceId == wp.Id);
        if (b == null)
            return new { workplaceId = wp.Id, workplaceName = wp.Name, status = "Idle",
                bookingType = (string?)null, operatorName = (string?)null, orderNumber = (string?)null,
                operationNumber = (string?)null, operationName = (string?)null, activityName = (string?)null,
                startedAt = (DateTime?)null, runtimeSeconds = 0 };
        return new {
            workplaceId = wp.Id, workplaceName = wp.Name,
            status = b.Status.ToString(), bookingType = b.BookingType.ToString(),
            operatorName = b.BdeOperator.DisplayName,
            orderNumber = b.WorkOperation?.ProductionOrder.OrderNumber,
            operationNumber = b.WorkOperation?.OperationNumber,
            operationName = b.WorkOperation?.Name,
            activityName = b.BdeActivity?.Name,
            startedAt = (DateTime?)b.StartedAt,
            runtimeSeconds = (int)(DateTime.Now - b.StartedAt).TotalSeconds
        };
    });

    return Ok(new { workplaces = tiles, serverTime = DateTime.Now });
}
```

Für die Injection des `IProductionWorkplaceRepository _workplaces` muss der Ctor von `BdeApiController` erweitert werden.

- [ ] **Step 2: Controller**

```csharp
using IdealAkeWms.Filters;
using Microsoft.AspNetCore.Mvc;

namespace IdealAkeWms.Controllers;

[RequireBdeShiftleadAccess]
public class BdeCockpitController : Controller
{
    public IActionResult Index() => View();
}
```

- [ ] **Step 3: View**

```html
@{ ViewData["Title"] = "BDE-Cockpit"; }

<h2>BDE-Cockpit</h2>
<div class="text-muted small mb-3">Live-Status aller Werkbänke · Server: <span id="serverTime">—</span></div>
<div id="cockpitGrid" class="row g-3"></div>

@section Scripts {
    <script src="~/js/bde-cockpit.js" asp-append-version="true"></script>
}
```

- [ ] **Step 4: JavaScript**

`bde-cockpit.js`:

```javascript
(function () {
    'use strict';
    const grid = document.getElementById('cockpitGrid');
    const serverTimeEl = document.getElementById('serverTime');

    function colorFor(tile) {
        if (tile.status === 'Idle') return 'bg-secondary';
        if (tile.status === 'Paused') return 'bg-warning';
        switch (tile.bookingType) {
            case 'Setup': return 'bg-orange';
            case 'Production': return 'bg-success';
            case 'Activity': return 'bg-info';
            default: return 'bg-secondary';
        }
    }

    function fmt(secs) {
        const h = Math.floor(secs/3600), m = Math.floor((secs%3600)/60);
        return h > 0 ? `${h}h ${m}m` : `${m}m`;
    }

    function render(data) {
        grid.innerHTML = data.workplaces.map(t => {
            if (t.status === 'Idle') {
                return `<div class="col-md-4 col-lg-3"><div class="card text-white ${colorFor(t)}">
                    <div class="card-body"><h5>${t.workplaceName}</h5><em>Frei</em></div></div></div>`;
            }
            const target = t.orderNumber
                ? `${t.orderNumber} / ${t.operationNumber} — ${t.operationName}`
                : t.activityName;
            return `<div class="col-md-4 col-lg-3"><div class="card text-white ${colorFor(t)}">
                <div class="card-body">
                    <h5>${t.workplaceName}</h5>
                    <div><strong>${t.bookingType}</strong> · ${t.status}</div>
                    <div class="small">${target ?? ''}</div>
                    <div class="small">${t.operatorName}</div>
                    <div class="small">Läuft seit: ${fmt(t.runtimeSeconds)}</div>
                </div></div></div>`;
        }).join('');
        serverTimeEl.textContent = new Date(data.serverTime).toLocaleTimeString();
    }

    async function tick() {
        try {
            const r = await fetch('/api/bde/cockpit');
            if (r.ok) render(await r.json());
        } catch (e) { /* ignore transient */ }
    }

    tick();
    setInterval(tick, 5000);
})();
```

`.bg-orange { background: var(--ake-orange); }` in `bde.css` ergänzen.

- [ ] **Step 5: Manuelle QA + Commit**

- Cockpit-Seite öffnen, parallel Terminal-Buchung starten → Kachel wechselt Farbe innerhalb 5 Sek.
- Pause → gelb. Finish → grau.

```bash
git add ...
git commit -m "feat(bde): add cockpit UI with 5s polling"
```

---

## Task 22: BdeBookingsController — Liste

**Files:**
- Create: `IdealAkeWms/Controllers/BdeBookingsController.cs`
- Create: `IdealAkeWms/Views/BdeBookings/Index.cshtml`
- Create: `IdealAkeWms/Models/ViewModels/BdeBookingListViewModel.cs`

- [ ] **Step 1: ViewModel + Controller-Index**

`BdeBookingListViewModel`:

```csharp
namespace IdealAkeWms.Models.ViewModels;

public class BdeBookingListViewModel
{
    public int Id { get; set; }
    public string OperatorName { get; set; } = "";
    public string WorkplaceName { get; set; } = "";
    public string BookingType { get; set; } = "";
    public string Status { get; set; } = "";
    public DateTime StartedAt { get; set; }
    public DateTime? EndedAt { get; set; }
    public string? Target { get; set; }
    public decimal TotalGood { get; set; }
    public decimal TotalScrap { get; set; }
    public bool IsCancelled { get; set; }
}
```

Controller (Index + Filter-Query-Params):

```csharp
[RequireBdeShiftleadAccess]
public class BdeBookingsController : Controller
{
    private readonly IBdeBookingRepository _repo;
    // ...

    public async Task<IActionResult> Index(int skip = 0, int take = 50, int? operatorId = null, int? workplaceId = null, DateTime? from = null, DateTime? to = null)
    {
        var list = await _repo.GetHistoryAsync(skip, take, operatorId, workplaceId, from, to);
        var vms = list.Select(b => new BdeBookingListViewModel {
            Id = b.Id,
            OperatorName = b.BdeOperator.DisplayName,
            WorkplaceName = b.ProductionWorkplace.Name,
            BookingType = b.BookingType.ToString(),
            Status = b.Status.ToString(),
            StartedAt = b.StartedAt, EndedAt = b.EndedAt,
            Target = b.WorkOperation != null
                ? $"{b.WorkOperation.ProductionOrder.OrderNumber}/{b.WorkOperation.OperationNumber}"
                : b.BdeActivity?.Name,
            TotalGood = b.Quantities.Sum(q => q.GoodQuantity),
            TotalScrap = b.Quantities.Sum(q => q.ScrapQuantity),
            IsCancelled = b.IsCancelled
        }).ToList();
        return View(vms);
    }
}
```

- [ ] **Step 2: View**

Tabelle mit Filter-Formular (Date-Picker für From/To, Operator-Dropdown, Workplace-Dropdown), filterable-table-Pattern aus bestehenden Views (z.B. `StockMovements/Index.cshtml`). Spalten: Start, Ende, Operator, Werkbank, Typ, Ziel, Gut, Ausschuss, Status, Aktionen (Edit, Storno — siehe Task 23).

- [ ] **Step 3: Commit**

```bash
git commit -m "feat(bde): add bookings list with filters"
```

---

## Task 23: Buchungs-Korrektur + Storno + Manuelles Beenden

**Files:**
- Modify: `IdealAkeWms/Controllers/BdeBookingsController.cs`
- Create: `IdealAkeWms/Models/ViewModels/BdeBookingEditViewModel.cs`
- Create: `IdealAkeWms/Views/BdeBookings/Edit.cshtml`

- [ ] **Step 1: ViewModel**

```csharp
public class BdeBookingEditViewModel
{
    public int Id { get; set; }
    [Display(Name = "Operator")] public int BdeOperatorId { get; set; }
    [Display(Name = "Werkbank")] public int ProductionWorkplaceId { get; set; }
    [Display(Name = "Start")] public DateTime StartedAt { get; set; }
    [Display(Name = "Ende")] public DateTime? EndedAt { get; set; }
    [Display(Name = "Status")] public BdeBookingStatus Status { get; set; }
    public List<BdeBookingQuantityEditRow> Quantities { get; set; } = new();
}

public class BdeBookingQuantityEditRow
{
    public int Id { get; set; }
    public decimal GoodQuantity { get; set; }
    public decimal ScrapQuantity { get; set; }
    public bool IsFinal { get; set; }
    public DateTime ReportedAt { get; set; }
    public bool Delete { get; set; }
}
```

- [ ] **Step 2: Actions (Edit GET+POST, Cancel POST, ManualClose POST)**

```csharp
[RequireBdeAdminAccess]
[HttpGet("BdeBookings/Edit/{id:int}")]
public async Task<IActionResult> Edit(int id)
{
    var b = await _repo.GetByIdAsync(id);
    if (b == null) return NotFound();
    // Map to ViewModel …
    return View(vm);
}

[RequireBdeAdminAccess]
[HttpPost("BdeBookings/Edit/{id:int}")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> Edit(int id, BdeBookingEditViewModel vm)
{
    if (!ModelState.IsValid) return View(vm);
    var b = await _repo.GetByIdAsync(id);
    if (b == null) return NotFound();
    b.BdeOperatorId = vm.BdeOperatorId;
    b.ProductionWorkplaceId = vm.ProductionWorkplaceId;
    b.StartedAt = vm.StartedAt;
    b.EndedAt = vm.EndedAt;
    b.Status = vm.Status;
    b.ModifiedAt = DateTime.Now;
    b.ModifiedBy = _userSvc.GetDisplayName();
    b.ModifiedByWindows = _userSvc.GetWindowsUserName();

    // Quantities aktualisieren (Löschen/Ändern/Hinzufügen)
    foreach (var row in vm.Quantities)
    {
        var q = b.Quantities.FirstOrDefault(x => x.Id == row.Id);
        if (q == null) continue;
        if (row.Delete) { _ctx.BdeBookingQuantities.Remove(q); continue; }
        q.GoodQuantity = row.GoodQuantity; q.ScrapQuantity = row.ScrapQuantity;
        q.IsFinal = row.IsFinal; q.ReportedAt = row.ReportedAt;
    }
    await _repo.UpdateAsync(b);
    TempData["SuccessMessage"] = "Buchung aktualisiert.";
    return RedirectToAction(nameof(Index));
}

[RequireBdeAdminAccess]
[HttpPost, ValidateAntiForgeryToken]
public async Task<IActionResult> Cancel(int id, string reason)
{
    var b = await _repo.GetByIdAsync(id);
    if (b == null) return NotFound();
    b.IsCancelled = true;
    b.CancellationReason = reason;
    b.ModifiedAt = DateTime.Now;
    b.ModifiedBy = _userSvc.GetDisplayName();
    b.ModifiedByWindows = _userSvc.GetWindowsUserName();
    await _repo.UpdateAsync(b);
    TempData["SuccessMessage"] = "Buchung storniert.";
    return RedirectToAction(nameof(Index));
}

[RequireBdeAdminAccess]
[HttpPost, ValidateAntiForgeryToken]
public async Task<IActionResult> ManualClose(int id, DateTime endedAt)
{
    var b = await _repo.GetByIdAsync(id);
    if (b == null) return NotFound();
    if (b.EndedAt != null) return BadRequest("Buchung bereits beendet.");
    b.EndedAt = endedAt;
    b.Status = BdeBookingStatus.Finished;
    b.ModifiedAt = DateTime.Now;
    b.ModifiedBy = _userSvc.GetDisplayName();
    b.ModifiedByWindows = _userSvc.GetWindowsUserName();
    await _repo.UpdateAsync(b);
    TempData["SuccessMessage"] = "Buchung manuell geschlossen.";
    return RedirectToAction(nameof(Index));
}
```

- [ ] **Step 3: Edit-View**

Tabelle mit editierbaren Quantity-Zeilen, Delete-Checkbox pro Zeile, Bootstrap-Form.

- [ ] **Step 4: Storno-Button in Liste**

In `Index.cshtml`: pro Zeile (falls `User.Admin` oder `bde_admin`) Button "Stornieren" mit Modal-Dialog für Grund. "Manuell schließen"-Button nur bei `EndedAt == null`.

- [ ] **Step 5: Filter in Liste für Storno**

In `GetHistoryAsync` default `includeCancelled = false`, Checkbox in Filter-Form "Stornierte anzeigen" setzt Query-Param.

- [ ] **Step 6: Commit**

```bash
git commit -m "feat(bde): add booking correction, cancel, manual close"
```

---

## Task 24: Navigation einbinden

**Files:**
- Modify: `IdealAkeWms/Views/Shared/_Layout.cshtml`

- [ ] **Step 1: Menüpunkte ergänzen**

Im bestehenden Navbar-Dropdown (entsprechend Berechtigungen sichtbar):

```cshtml
@{
    var canUseBde = await CurrentUserService.CanUseBdeAsync();
    var canManageBdeShiftlead = await CurrentUserService.CanManageBdeShiftleadAsync();
    var canManageBdeAdmin = await CurrentUserService.CanManageBdeAdminAsync();
}
@if (canUseBde || canManageBdeShiftlead)
{
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">BDE</a>
        <ul class="dropdown-menu">
            @if (canUseBde) { <li><a class="dropdown-item" asp-controller="BdeTerminal" asp-action="Index">Terminal</a></li> }
            @if (canManageBdeShiftlead) {
                <li><a class="dropdown-item" asp-controller="BdeCockpit" asp-action="Index">Cockpit</a></li>
                <li><a class="dropdown-item" asp-controller="BdeBookings" asp-action="Index">Buchungen</a></li>
                <li><a class="dropdown-item" asp-controller="BdeMasterData" asp-action="Index">Stammdaten</a></li>
            }
        </ul>
    </li>
}
```

(Injection für `ICurrentUserService` in Layout über `@inject`.)

- [ ] **Step 2: Manuelle QA**

- Als Admin: alle Menü-Einträge sichtbar.
- Als User ohne BDE-Rolle: BDE-Menü nicht sichtbar.
- Als `bde_user`: nur Terminal sichtbar.

- [ ] **Step 3: Commit**

```bash
git commit -m "feat(bde): add BDE navigation menu"
```

---

## Task 25: Session Sliding Expiration verifizieren

**Files:**
- Read: `IdealAkeWms/Program.cs` (Session-Konfiguration)

- [ ] **Step 1: Konfiguration prüfen**

In `Program.cs` die `AddSession`-Konfiguration öffnen. `IdleTimeout` sollte gesetzt sein. Session Sliding ist in ASP.NET Core `Session` **default aktiviert** — jeder Request verlängert die Session um `IdleTimeout` ab dem Request-Zeitpunkt. Das ist das gewünschte Verhalten.

- [ ] **Step 2: Explizit validieren**

Manuell testen:
- Einloggen, 7h warten (oder `IdleTimeout` temporär auf 2min setzen), dazwischen alle 1min AJAX-Call (das Polling im Cockpit tut das automatisch alle 5s) → Session sollte nicht ablaufen.
- Einloggen, 2x `IdleTimeout`-Dauer *ohne* Request → Session läuft ab (erwartet).

Keine Code-Änderung — nur Verifikation.

- [ ] **Step 3: Session-Expiry im Terminal anzeigen**

Im Terminal-View (Task 20) das `sessionExpiry`-Span aus dem JS befüllen. Einfachster Weg: Server liefert den Wert als `<input type="hidden" id="sessionExpiresAt" value="@(DateTime.Now + sessionTimeout)" />`, JS rendert:

```javascript
const exp = new Date(document.getElementById('sessionExpiresAt').value);
document.getElementById('sessionExpiry').textContent = exp.toLocaleTimeString();
```

Bei jedem API-Call Expiry neu vom Server holen (im Response-Header `X-Session-Expires-At` mitgeben — optional, Phase 2).

Für Phase 1: einfacher statischer Wert (Session-Start + IdleTimeout).

- [ ] **Step 4: Commit**

```bash
git commit -m "feat(bde): display terminal session expiry"
```

---

## Task 26: Version + Changelog + Help

**Files:**
- Modify: `IdealAkeWms/AppVersion.cs`
- Modify: `IdealAkeWms.Service/AppVersion.cs`
- Modify: `IdealAkeWms/Views/Help/Changelog.cshtml`
- Modify: `IdealAkeWms/Views/Help/Index.cshtml`

- [ ] **Step 1: Version hochzählen**

Bestehende Version lesen, Minor erhöhen (BDE ist großes Feature). Z.B. von `1.8.0` → `1.9.0`.

```csharp
public const string Version = "1.9.0";
public const string ReleaseDate = "2026-04-14";
```

Beide Dateien identisch halten.

- [ ] **Step 2: Changelog-Eintrag**

In `Changelog.cshtml` neuen Karten-Block (Corporate-Design, wie vorhandene Einträge):

```cshtml
<div class="changelog-card">
    <h3>Version 1.9.0 — 14.04.2026</h3>
    <h4>BDE Phase 1 — Betriebsdatenerfassung</h4>
    <ul>
        <li>Neues BDE-Terminal mit Scan-basierter Buchung (Personalnummer + FA/AG)</li>
        <li>Statusverwaltung: Rüsten, Produktion, Pause, Fortsetzen, Beenden mit Mengen-Erfassung</li>
        <li>Teilfertigmeldungen (Gutmenge + Ausschuss) mitten in laufender Produktion</li>
        <li>Ungeplante Tätigkeiten (Wartung, Reinigung, …) via konfigurierbare Kategorien</li>
        <li>Live-Cockpit für Schichtleiter und Admins (5-Sek-Refresh aller Werkbänke)</li>
        <li>BDE-Stammdaten: Operatoren, Aktivitäts-Kategorien, Terminal-Konfigurationen</li>
        <li>Admin-Korrekturen: Buchungs-Editor, Storno mit Grund, manuelles Schließen vergessener Buchungen</li>
        <li>Drei neue Rollen: bde_user, bde_shiftlead, bde_admin</li>
    </ul>
</div>
```

- [ ] **Step 3: Hilfeseite**

In `Help/Index.cshtml` neuen Abschnitt "BDE" mit Rollen-Erklärung und Kurz-Anleitung zum Terminal-Flow.

- [ ] **Step 4: Commit**

```bash
git commit -m "docs(bde): bump version to 1.9.0, add changelog and help"
```

---

## Task 27: CLAUDE.md + PROJECT_STATUS + README

**Files:**
- Modify: `CLAUDE.md`
- Modify: `PROJECT_STATUS.md`
- Modify: `README.md`

- [ ] **Step 1: CLAUDE.md erweitern**

Im Abschnitt "Rollenkonzept" die 3 neuen Rollen ergänzen. In "Controller-Muster"/"Zugriffsschutz"-Tabelle die 3 neuen Filter-Attribute eintragen. Im Abschnitt "Bekannte Fallstricke" ergänzen:
- **BDE-Buchung ein-Operator-Regel**: gefilterter Unique-Index `IX_BdeBookings_BdeOperatorId_Active` verhindert mehrere offene Buchungen pro Operator. Starten eines neuen AG auf laufender Production → Service gibt `QuantityRequired` zurück, Controller zeigt Mengen-Dialog.
- **BDE-Paused Semantik**: Paused hat `EndedAt = gesetzt`. Fortsetzung erzeugt neue Buchung mit `ParentBookingId`. Cockpit-Query `WHERE EndedAt IS NULL` ignoriert Paused — Cockpit zeigt nur Running.

- [ ] **Step 2: PROJECT_STATUS.md**

Neues Kapitel "BDE Phase 1 (v1.9.0)" mit Kurz-Feature-Liste.

- [ ] **Step 3: README.md**

Im Feature-Liste-Abschnitt "Betriebsdatenerfassung (BDE)" ergänzen.

- [ ] **Step 4: Commit**

```bash
git commit -m "docs(bde): update CLAUDE.md, PROJECT_STATUS, README"
```

---

## Task 28: Integrations-QA + SQL-Validation

**Files:** keine

- [ ] **Step 1: Fresh Install testen**

Lokale DB neu anlegen, `SQL/00_FreshInstall.sql` ausführen. Verifizieren:
- Alle 5 BDE-Tabellen vorhanden
- Alle Indizes vorhanden (`SELECT * FROM sys.indexes WHERE object_id IN (OBJECT_ID('BdeBookings'), ...)`)
- Alle Check-Constraints vorhanden (`SELECT * FROM sys.check_constraints WHERE parent_object_id = OBJECT_ID('BdeBookings')`)
- 3 neue Rollen in `Roles`

- [ ] **Step 2: Upgrade testen**

Bestehende DB (Stand vor BDE), `SQL/42_AddBde.sql` ausführen. Identische Verifikation wie Step 1.

- [ ] **Step 3: Check-Constraint-Validierung**

Gegen echte SQL-DB folgende Inserts versuchen (müssen scheitern):

```sql
-- Weder WorkOperation noch Activity gesetzt → CK_BdeBookings_Target
INSERT INTO BdeBookings (...) VALUES (..., WorkOperationId = NULL, BdeActivityId = NULL, ...);

-- Running mit EndedAt gesetzt → CK_BdeBookings_StatusEnded
INSERT INTO BdeBookings (..., Status = 1, EndedAt = GETDATE(), ...);

-- Zweite Running auf selben AG → IX_BdeBookings_WorkOperationId_Active
-- (zwei Inserts mit gleicher WorkOperationId, EndedAt = NULL, IsCancelled = 0)
```

Alle müssen mit SQL-Error fehlschlagen.

- [ ] **Step 4: End-to-End-Flow**

Einmal ein komplettes Szenario durchspielen:
1. Operator scannt, startet Rüsten auf FA-123/10
2. Operator beendet Rüsten, startet Produktion
3. Operator meldet Teilmenge (5 gut, 0 Ausschuss)
4. Operator pausiert
5. Zweiter Operator scannt denselben AG → System bietet Fortsetzung
6. Zweiter Operator produziert weiter, beendet mit Abschluss-Mengen (20 gut, 2 Ausschuss)
7. Cockpit zeigt während aller Schritte korrekt
8. Buchungsliste zeigt 3 Buchungen (Rüsten, Production-Pause, Production-Ende) verknüpft über ParentBookingId
9. Admin öffnet Edit auf mittlerer Buchung, korrigiert Teilmenge auf 6 → Summen in Liste passen

- [ ] **Step 5: Commit (falls Fixes nötig)**

Falls in QA Probleme auftauchen: Fix-Tasks inline anhängen, dann:

```bash
git commit -m "fix(bde): resolve issues found in integration QA"
```

---

## Self-Review Ergebnis

**Spec coverage:**
- ✅ Akteure & Rollen (Tasks 1, 8, 9, 15, 24)
- ✅ Terminal/Operator/Activity/Booking/Quantity-Entities (Tasks 2, 3)
- ✅ DB-Constraints, Filter-Indizes (Task 4, 5, 6)
- ✅ State-Machine (Task 14, 18+ Tests)
- ✅ Terminal-UI mit Scan + Modals (Task 20)
- ✅ Cockpit-Polling (Task 21)
- ✅ Stammdaten-CRUD (Tasks 16, 17, 18)
- ✅ Buchungsliste + Korrektur + Storno + manuelles Schließen (Tasks 22, 23)
- ✅ Session Sliding (Task 25)
- ✅ Versions-, Changelog-, Help-Updates (Task 26)
- ✅ CLAUDE.md (Task 27)
- ✅ Integration-QA (Task 28)

**Placeholder scan:** Keine TBDs. "Implementation analog Task X" in Tasks 11, 12, 17, 18 ist bewusst — Code-Pattern identisch, nur Entity-Typ wechselt.

**Type consistency:** Enum-Werte (`BdeBookingType`, `BdeBookingStatus`), Method-Signaturen (`StartSetupAsync(int, int, int, int)` etc.), ViewModel-Property-Namen durchgehend konsistent.

---

## Ausführungs-Optionen

**Plan gespeichert in `docs/superpowers/plans/2026-04-14-bde-phase-1.md`.**

1. **Subagent-Driven (empfohlen)** — Fresh Subagent pro Task, Review zwischen Tasks, schnelle Iteration
2. **Inline Execution** — Tasks in dieser Session via executing-plans, Batch mit Checkpoints

Welche Ausführung?
